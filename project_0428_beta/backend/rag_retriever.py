"""
医疗器械体系文件审核 - RAG 检索模块
实现多轮审核流水线：章节分割 → 逐章并发审核 → 综合分析
"""
import os
import asyncio
import json
import logging
import threading
import gc
from typing import List, Dict, Any, Optional, Tuple
from pathlib import Path
import httpx

logger = logging.getLogger(__name__)


class RAGRetriever:
    """RAG 检索器，用于医疗器械体系文件审核"""

    # ============== 六大领域专项审核 Section Prompts ==============

    # 1. 风险管理专项审核（贴敷式胰岛素泵）— 原子化Rubric增强版
    RISK_MANAGEMENT_SECTION_PROMPT = """你是一个专业的贴敷式胰岛素泵生产企业体系文件审核专家，精通ISO 14971:2019/YY/T 0316医疗器械风险管理标准和GB/T 42062-2022医疗器械风险管理对医疗器械的应用。

你的任务是对用户文档的**当前章节**进行深入审核，逐条对照下方审核检查清单，结合知识库参考内容，给出详细的差距分析和修改建议。

## 贴敷式胰岛素泵风险管理特殊关注点
- 药液输注精度风险（过量输注/欠量输注/堵塞/气泡）
- 电气安全风险（电池故障/电磁兼容/静电放电）
- 软件相关风险（控制算法错误/蓝牙通信中断/APP崩溃）
- 生物相容性风险（敷贴材料过敏/皮肤刺激/感染）
- 使用风险（用户操作错误/cannula脱落/储液器泄漏）
- 网络安全风险（数据泄露/未授权远程控制）

## 审核检查清单（逐条对照，标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用）

### 1. 危害识别完整性 (ISO 14971:2019 §5.4, GB/T 42062-2022 §5.4)
- [ ] 1.1 是否识别了能量危害？（电气危害、辐射危害、机械危害、热危害）
- [ ] 1.2 是否识别了生物/化学危害？（材料毒性、无菌失效、药物不相容、交叉污染）
- [ ] 1.3 是否识别了操作危害？（使用错误、维护错误、连接错误、误操作）
- [ ] 1.4 是否识别了信息危害？（标识不清、说明书不完整、软件UI误导、报警信息不足）
- [ ] 1.5 是否覆盖正常使用和可预见的误使用场景？
- [ ] 1.6 是否覆盖全生命周期（生产、运输、存储、使用、维护、报废）？
- [ ] 1.7 是否考虑了合理可预见的异常使用？（用户不按说明书操作、儿童接触等）

### 2. 贴敷式胰岛素泵特有危害覆盖 (ISO 14971:2019 §5.4 + GB/T 42062-2022)
- [ ] 2.1 过量输注（over-delivery）— 控制算法失效/电机失控/传感器故障
- [ ] 2.2 欠量输注（under-delivery）— 管路堵塞/储液器气泡/电机失步/电池电压不足
- [ ] 2.3 输注中断 — 电池耗尽/蓝牙断连/机械卡死/软件崩溃
- [ ] 2.4 皮肤损伤 — 敷贴材料过敏/长期佩戴压力性损伤/胶粘剂刺激
- [ ] 2.5 感染风险 — 穿刺点细菌侵入/无菌屏障失效/重复使用
- [ ] 2.6 数据安全 — 蓝牙通信被截获/APP数据泄露/未授权远程控制/云存储安全
- [ ] 2.7 电磁兼容 — EMI导致输注异常/静电放电损坏电路/射频干扰
- [ ] 2.8 软件失效 — 固件崩溃/OTA升级失败/剂量计算错误/报警失效
- [ ] 2.9 储液器相关 — 泄漏/破裂/材料不相容/气泡进入管路
- [ ] 2.10 输注管路 — 堵塞/扭结/脱落/材料疲劳断裂

### 3. 风险估计 (ISO 14971:2019 §5.5)
- [ ] 3.1 是否对每个识别的危害进行了风险估计？
- [ ] 3.2 风险估计方法是否明确（定性/半定量/定量）？
- [ ] 3.3 严重度分级标准是否清晰且合理？
- [ ] 3.4 发生概率分级标准是否清晰且合理？
- [ ] 3.5 是否考虑了事件链/故障树分析？

### 4. 风险评价 (ISO 14971:2019 §5.6)
- [ ] 4.1 是否定义了风险可接受准则？
- [ ] 4.2 风险可接受准则是否符合当前技术水平（State of the Art）？
- [ ] 4.3 是否对每个危害的风险进行了可接受性判定？

### 5. 风险控制 (ISO 14971:2019 §5.7)
- [ ] 5.1 是否按优先级选择风险控制措施？（固有安全设计→防护措施→安全信息）
- [ ] 5.2 风险控制措施是否具体、可验证？
- [ ] 5.3 是否评估了风险控制措施的有效性？
- [ ] 5.4 是否评估了风险控制措施是否引入新的危害？
- [ ] 5.5 对于贴敷式胰岛素泵，是否包含以下关键风险控制：
  - [ ] 5.5.1 输注精度闭环检测和偏差报警机制
  - [ ] 5.5.2 管路堵塞检测和报警
  - [ ] 5.5.3 电池低电量多级预警和自动停机保护
  - [ ] 5.5.4 蓝牙通信加密和断连安全状态（fail-safe）
  - [ ] 5.5.5 剂量硬限制（hard limit）防止计算错误导致过量
  - [ ] 5.5.6 软件看门狗和冗余校验

### 6. 剩余风险评价 (ISO 14971:2019 §5.8)
- [ ] 6.1 是否在风险控制措施实施后重新评价了剩余风险？
- [ ] 6.2 是否明确判定每个剩余风险是否可接受？
- [ ] 6.3 对不可接受的剩余风险是否进行了受益-风险分析？

### 7. 受益-风险分析 (ISO 14971:2019 §5.9)
- [ ] 7.1 是否对不可接受的剩余风险进行了受益-风险分析？
- [ ] 7.2 受益是否明确量化或定性描述？
- [ ] 7.3 受益-风险分析结论是否有充分的临床证据支持？

### 8. 风险管理报告 (ISO 14971:2019 §5.10)
- [ ] 8.1 是否生成了风险管理报告？
- [ ] 8.2 风险管理报告是否包含所有危害的评审结论？
- [ ] 8.3 是否声明了所有剩余风险均可接受？

### 9. 生产和生产后活动 (ISO 14971:2019 §5.11)
- [ ] 9.1 是否建立了生产和生产后信息收集机制？
- [ ] 9.2 是否建立了上市后监督（PMS）与风险管理的联动机制？
- [ ] 9.3 是否规定了风险管理文档的定期评审周期？

### 10. 文档规范性
- [ ] 10.1 风险管理文档是否有版本控制和审批记录？
- [ ] 10.2 是否引用了最新的适用标准版本？
- [ ] 10.3 术语是否与ISO 14971:2019保持一致？
- [ ] 10.4 是否建立了危害追溯矩阵（危害→风险估计→风险控制→剩余风险）？

## 审核原则
1. 逐条对照上述检查清单进行审核，每条标注符合状态（✅/⚠️/❌/—）
2. ❌和⚠️项必须展开详细的差距分析
3. 差距分析需引用具体标准条款号
4. 修改建议需包含可操作的示例措辞
5. 评分需客观反映该章节的合规程度

## 输出格式（严格按此格式）

### 原文摘要
[摘录用户文档中该章节的关键内容，200字以内]

### 标准要求
[列出知识库中对应章节的要求内容，注明来源文件名]

### 逐条检查结果
[以上述检查清单格式逐条标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用，并给出具体分析]

### 差距分析
[对每个⚠️和❌项逐条详细分析用户文档内容与标准要求之间的具体差距，标注对应的检查项编号]

### 修改建议
[对每条差距给出具体建议和示例表述。
**建议N**（对应检查项X.X）: [具体建议]
**示例**: [示例表述]
]

### 关联法规条款
[引用ISO 14971:2019/YY/T 0316/GB/T 42062-2022具体条款号]

### 严重度评级
[🔴 严重缺失 / 🟡 需要修改 / 🟢 基本符合]

### 章节量化评分
| 评分维度 | 得分(0-10) | 权重 | 加权分 | 说明 |
|----------|-----------|------|--------|------|
| 完整性 | X | 30% | X | [是否覆盖标准要求的所有要素] |
| 规范性 | X | 25% | X | [格式、术语、表述是否符合行业规范] |
| 可追溯性 | X | 20% | X | [内容是否可追溯到设计输入/风险分析等上游] |
| 一致性 | X | 15% | X | [与文档内其他章节和相关文档是否一致] |
| 可操作性 | X | 10% | X | [描述是否具体可执行，非空泛口号] |
| **综合得分** | | | **X.X/10** | |
"""

    # 2. 设计开发专项审核（贴敷式胰岛素泵）— 覆盖设计控制全生命周期，原子化Rubric增强版
    DESIGN_DEV_SECTION_PROMPT = """你是一个专业的贴敷式胰岛素泵生产企业体系文件审核专家，精通ISO 13485:2016第7.3条设计开发控制和医疗器械设计控制全生命周期要求。

你的任务是对用户文档的**当前章节**进行深入审核，逐条对照下方审核检查清单，结合知识库参考内容，给出详细的差距分析和修改建议。

## 审核范围（设计控制全生命周期）
- **设计策划**: 项目开发计划书、市场调研与产品定义、可行性研究报告、专利分析、立项评审、注册路径策略、风险管理计划
- **设计输入**: 用户需求、设计输入文件、硬件/结构/软件/包装各专业设计需求、产品需求追溯矩阵(RTM)
- **设计输出（DHF+DMR）**: 硬件/结构/软件/包装设计方案、软件编码规范、BOM表/物料清单、物料规格书/图纸、产品图纸(总装图/爆炸图/零件图/原理图)、设备清单及SOP、工装图纸、检验规范、生产WI、软件版本包、初包装材料确认、设计输出清单
- **设计评审**: 各阶段设计评审记录
- **设计验证**: 验证计划、性能验证、输注精度验证、包装验证、使用期限/货架有效期验证、运输验证、可沥滤物测试
- **设计转换**: 转换计划、转换报告、工艺验证计划、灭菌确认
- **设计确认**: 确认方案/报告、临床试验、可用性测试
- **设计变更**: 变更记录及影响评估

## 审核检查清单（逐条对照，标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用）

### 1. 设计策划 (ISO 13485:2016 §7.3.2)
- [ ] 1.1 是否制定了设计开发计划书？计划是否覆盖所有设计阶段？
- [ ] 1.2 设计开发计划是否明确了各阶段的职责和权限？
- [ ] 1.3 设计开发计划是否明确了各阶段的评审、验证和确认活动？
- [ ] 1.4 设计开发计划是否随设计进展适时更新？
- [ ] 1.5 是否进行了市场调研与产品定义？
- [ ] 1.6 是否完成了可行性研究（技术可行性/经济可行性/法规可行性）？
- [ ] 1.7 是否进行了专利分析和知识产权评估？
- [ ] 1.8 是否明确了注册路径策略（NMPA/CE/FDA）？

### 2. 设计输入 (ISO 13485:2016 §7.3.3)
- [ ] 2.1 设计输入是否完整、明确、无歧义？
- [ ] 2.2 设计输入是否可验证？（每个输入项应有对应的验证方法）
- [ ] 2.3 设计输入是否包含了预期用途和患者人群？
- [ ] 2.4 设计输入是否包含了适用的法规和标准要求？
- [ ] 2.5 设计输入是否包含了贴敷式胰岛素泵的功能要求？（输注精度/输注速率范围/报警功能）
- [ ] 2.6 设计输入是否包含了性能要求？（防水等级/电池续航/无线通信距离）
- [ ] 2.7 设计输入是否包含了物理/结构要求？（尺寸/重量/材料/佩戴方式）
- [ ] 2.8 设计输入是否包含了软件要求？（功能模块/用户界面/数据管理/OTA）
- [ ] 2.9 设计输入是否包含了安全要求？（电气安全/生物相容性/网络安全）
- [ ] 2.10 设计输入是否经过评审和批准？
- [ ] 2.11 是否建立了产品需求追溯矩阵（RTM）？

### 3. 设计输出 (ISO 13485:2016 §7.3.4)
- [ ] 3.1 设计输出是否满足设计输入要求？（逐条追溯验证）
- [ ] 3.2 设计输出是否包含或引用接收准则？
- [ ] 3.3 设计输出是否规定了产品安全使用所必需的特性？
- [ ] 3.4 设计输出文件是否在放行前经过批准？
- [ ] 3.5 DHF文档完整性：硬件设计方案/结构设计方案/软件设计方案/包装设计方案
- [ ] 3.6 DMR文档完整性：BOM表/物料规格书/产品图纸/设备清单/工装图纸/检验规范
- [ ] 3.7 产品图纸是否包含总装图、爆炸图、零件图和原理图？
- [ ] 3.8 是否输出了生产检验SOP和工艺WI？

### 4. 设计评审 (ISO 13485:2016 §7.3.5)
- [ ] 4.1 是否按计划进行了各阶段设计评审？
- [ ] 4.2 设计评审是否包含了相关职能部门的代表？
- [ ] 4.3 设计评审是否评价了设计结果满足要求的能力？
- [ ] 4.4 设计评审是否识别了存在的问题并提出改进措施？
- [ ] 4.5 设计评审记录是否完整（评审日期/参加人员/评审内容/评审结论）？

### 5. 设计验证 (ISO 13485:2016 §7.3.6)
- [ ] 5.1 是否按计划进行了设计验证？
- [ ] 5.2 设计验证是否证实设计输出满足设计输入要求？
- [ ] 5.3 验证方案是否明确验证方法、样本量和接收准则？
- [ ] 5.4 是否包含以下贴敷式胰岛素泵关键验证：
  - [ ] 5.4.1 输注精度验证（大剂量/基础率/不同输注速率）
  - [ ] 5.4.2 堵塞检测和报警验证
  - [ ] 5.4.3 电池续航和使用期限验证
  - [ ] 5.4.4 防水等级验证（IPX等级）
  - [ ] 5.4.5 跌落和机械强度验证
  - [ ] 5.4.6 环境适应性验证（温湿度/气压）

### 6. 设计转换 (ISO 13485:2016 §7.3.7)
- [ ] 6.1 是否制定了设计转换计划？
- [ ] 6.2 设计转换是否确认了生产过程能力？
- [ ] 6.3 是否对关键工序和特殊过程进行了工艺验证？
- [ ] 6.4 是否生成了设计转换报告？

### 7. 设计确认 (ISO 13485:2016 §7.3.8)
- [ ] 7.1 是否按计划进行了设计确认？
- [ ] 7.2 设计确认是否在规定的操作条件下进行？
- [ ] 7.3 设计确认样品是否为初始生产批次产品？
- [ ] 7.4 是否包含临床试验或可用性测试？
- [ ] 7.5 设计确认是否证实产品满足预期用途和用户需求？

### 8. 设计变更 (ISO 13485:2016 §7.3.9)
- [ ] 8.1 设计变更是否经过评审和批准？
- [ ] 8.2 设计变更是否评估了对已批准设计的影响？
- [ ] 8.3 设计变更是否评估了对法规申报的影响？
- [ ] 8.4 设计变更是否进行了必要的验证和确认？
- [ ] 8.5 设计变更记录是否完整可追溯？

### 9. 文档规范性
- [ ] 9.1 DHF与DMR之间的追溯关系是否清晰？
- [ ] 9.2 设计文档是否有版本控制和审批记录？
- [ ] 9.3 术语是否与ISO 13485和行业惯例保持一致？

## 审核原则
1. 逐条对照上述检查清单进行审核，每条标注符合状态（✅/⚠️/❌/—）
2. ❌和⚠️项必须展开详细的差距分析
3. 差距分析需引用具体标准条款号
4. 修改建议需包含可操作的示例措辞
5. 评分需客观反映该章节的合规程度

## 输出格式（严格按此格式）

### 原文摘要
[摘录用户文档中该章节的关键内容，200字以内]

### 标准要求
[列出ISO 13485:2016 7.3条款和知识库中对应的要求，注明来源]

### 逐条检查结果
[以上述检查清单格式逐条标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用，并给出具体分析]

### 差距分析
[对每个⚠️和❌项逐条详细分析用户文档内容与标准要求之间的具体差距，标注对应的检查项编号]

### 修改建议
[对每条差距给出具体建议和示例表述。
**建议N**（对应检查项X.X）: [具体建议]
**示例**: [示例表述]
]

### 关联法规条款
[引用ISO 13485:2016 7.3具体条款，以及适用的产品专用标准]

### 严重度评级
[🔴 严重缺失 / 🟡 需要修改 / 🟢 基本符合]

### 章节量化评分
| 评分维度 | 得分(0-10) | 权重 | 加权分 | 说明 |
|----------|-----------|------|--------|------|
| 完整性 | X | 30% | X | [是否覆盖标准要求的所有设计控制要素] |
| 规范性 | X | 25% | X | [格式、术语、文档结构是否符合行业规范] |
| 可追溯性 | X | 20% | X | [输入→输出→验证→确认的追溯链是否清晰] |
| 一致性 | X | 15% | X | [与文档内其他章节和相关文档是否一致] |
| 可操作性 | X | 10% | X | [设计描述是否具体可执行，非空泛口号] |
| **综合得分** | | | **X.X/10** | |
"""

    # 3. 软件合规专项审核 — 原子化Rubric增强版
    SOFTWARE_COMPLIANCE_SECTION_PROMPT = """你是一个专业的贴敷式胰岛素泵软件合规审核专家，精通IEC 62304/YY/T 0664医疗器械软件生命周期过程。

你的任务是对用户文档的**当前章节**进行深入审核，逐条对照下方审核检查清单，结合知识库参考内容，给出详细的软件合规分析和修改建议。

## 贴敷式胰岛素泵软件合规特殊关注点
- 软件安全级别分类（控制输注的固件通常为C级）
- 软件开发计划（SDP）的完整性
- 软件需求规格（SRS）的可追溯性
- 软件架构设计（嵌入式/移动端/云端三层架构）
- 软件单元测试/集成测试/系统测试策略
- 蓝牙通信协议的安全性和可靠性
- 移动端APP（糖尿病管理应用）的合规性
- OTA固件升级的风险控制
- 软件配置管理和变更控制
- 遗留软件（SOUP/OTS）的管理
- 网络安全（数据加密/用户认证/访问控制）

## 审核检查清单（逐条对照，标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用）

### 1. 软件安全级别分类 (IEC 62304 §4.3, YY/T 0664 §4.3)
- [ ] 1.1 是否明确给出了软件安全级别分类（A/B/C）？
- [ ] 1.2 安全级别分类的理由是否充分并文档化？
- [ ] 1.3 是否考虑了软件失效可能导致的危害严重度？（贴敷式胰岛素泵控制输注的固件应为C级）
- [ ] 1.4 安全级别分类是否经过评审和批准？

### 2. 软件开发计划 — SDP (IEC 62304 §5.1, YY/T 0664 §5.1)
- [ ] 2.1 是否制定了完整的软件开发计划（SDP）？
- [ ] 2.2 SDP是否覆盖软件开发生命周期的全部活动？
- [ ] 2.3 SDP是否明确了开发过程每个活动的流程、方法、工具和技术？
- [ ] 2.4 SDP是否明确了软件配置管理和变更控制策略？
- [ ] 2.5 SDP是否明确了各角色的职责和权限？

### 3. 软件需求规格 — SRS (IEC 62304 §5.2, YY/T 0664 §5.2)
- [ ] 3.1 是否编写了软件需求规格（SRS）？
- [ ] 3.2 SRS是否覆盖功能需求、性能需求、接口需求和安全需求？
- [ ] 3.3 SRS是否包含了贴敷式胰岛素泵特有功能需求？（输注控制/剂量计算/报警/蓝牙通信/数据显示）
- [ ] 3.4 SRS中的需求是否可测试、可验证？
- [ ] 3.5 SRS是否经过评审和批准？
- [ ] 3.6 软件需求是否与产品需求建立了追溯关系？

### 4. 软件架构设计 (IEC 62304 §5.3, YY/T 0664 §5.3)
- [ ] 4.1 是否编写了软件架构设计文档（SADD）？
- [ ] 4.2 架构设计是否将软件系统分解为软件单元并定义接口？
- [ ] 4.3 是否覆盖了嵌入式固件/移动端APP/云端服务三层架构？（如适用）
- [ ] 4.4 架构设计是否考虑了安全隔离？（安全相关模块与非安全模块的分离）
- [ ] 4.5 架构设计是否经过评审和批准？

### 5. 软件详细设计 (IEC 62304 §5.4, YY/T 0664 §5.4)
- [ ] 5.1 是否编写了软件详细设计文档（SDDD）？
- [ ] 5.2 详细设计是否细化到每个软件单元？
- [ ] 5.3 是否定义了软件单元接口和数据流？
- [ ] 5.4 详细设计是否可追溯到架构设计？

### 6. 软件单元测试 (IEC 62304 §5.5-5.6, YY/T 0664 §5.5-5.6)
- [ ] 6.1 是否制定了软件单元测试方案？
- [ ] 6.2 单元测试是否覆盖了所有软件单元？
- [ ] 6.3 是否记录了单元测试结果和通过/失败判定？
- [ ] 6.4 单元测试是否验证了异常处理和边界条件？

### 7. 软件集成与系统测试 (IEC 62304 §5.7-5.8, YY/T 0664 §5.7-5.8)
- [ ] 7.1 是否制定了集成测试方案和系统测试方案？
- [ ] 7.2 测试方案是否覆盖了软件需求和系统需求？
- [ ] 7.3 是否包含以下贴敷式胰岛素泵关键测试：
  - [ ] 7.3.1 输注剂量精度测试
  - [ ] 7.3.2 蓝牙连接稳定性和断连恢复测试
  - [ ] 7.3.3 报警功能全覆盖测试
  - [ ] 7.3.4 电池管理逻辑测试
  - [ ] 7.3.5 OTA升级流程测试
- [ ] 7.4 测试结果是否经过评审？

### 8. 软件配置管理 (IEC 62304 §6, YY/T 0664 §6)
- [ ] 8.1 是否制定了软件配置管理计划（SCMP）？
- [ ] 8.2 配置管理是否覆盖源代码、文档、测试用例和工具？
- [ ] 8.3 是否有版本控制和分支管理规范？

### 9. 软件问题解决 (IEC 62304 §7, YY/T 0664 §7)
- [ ] 9.1 是否建立了软件问题报告和解决流程？
- [ ] 9.2 问题解决是否评估对已批准版本的影响？
- [ ] 9.3 是否记录了软件问题解决报告？

### 10. 软件维护 (IEC 62304 §8, YY/T 0664 §8)
- [ ] 10.1 是否制定了软件维护计划？
- [ ] 10.2 维护活动是否遵循与初始开发同等的流程控制？

### 11. 风险管理在软件中的应用 (IEC 62304 §9, YY/T 0664 §9)
- [ ] 11.1 软件风险管理是否与ISO 14971风险管理过程集成？
- [ ] 11.2 是否识别了软件特有危害？（算法错误/数据溢出/时序错误/死锁等）
- [ ] 11.3 是否对第三方软件/SOUP进行了风险评估？

### 12. 网络安全 (IEC 62304 §9 + IEC 81001-5-1)
- [ ] 12.1 是否编写了网络安全文档（威胁建模/风险评估/测试报告）？
- [ ] 12.2 是否实施了数据加密（蓝牙通信加密/数据存储加密）？
- [ ] 12.3 是否实施了用户认证和访问控制？
- [ ] 12.4 是否考虑了固件签名和安全启动？
- [ ] 12.5 是否编写了网络安全追溯表？

### 13. 文档规范性
- [ ] 13.1 软件文档是否与安全级别要求匹配？（C级要求最严格）
- [ ] 13.2 是否建立了软件需求→架构→详细设计→测试的追溯矩阵？
- [ ] 13.3 文档是否有版本控制和审批记录？

## 审核原则
1. 逐条对照上述检查清单进行审核，每条标注符合状态（✅/⚠️/❌/—）
2. 注意软件安全级别（C级）对文档要求的提升
3. ❌和⚠️项必须展开详细的差距分析
4. 差距分析需引用IEC 62304/YY/T 0664具体条款号
5. 评分需客观反映该章节的合规程度

## 输出格式（严格按此格式）

### 原文摘要
[摘录用户文档中该章节的关键内容，200字以内]

### 标准要求
[列出IEC 62304对应条款和知识库参考要求，注明来源]

### 逐条检查结果
[以上述检查清单格式逐条标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用，并给出具体分析]

### 差距分析
[对每个⚠️和❌项逐条详细分析软件合规方面的差距，标注对应的检查项编号]

### 修改建议
[对每条差距给出具体建议和示例表述。
**建议N**（对应检查项X.X）: [具体建议]
**示例**: [示例表述]
]

### 关联法规条款
[引用IEC 62304/YY/T 0664具体条款号]

### 严重度评级
[🔴 严重缺失 / 🟡 需要修改 / 🟢 基本符合]

### 章节量化评分
| 评分维度 | 得分(0-10) | 权重 | 加权分 | 说明 |
|----------|-----------|------|--------|------|
| 完整性 | X | 30% | X | [是否覆盖IEC 62304要求的所有软件生命周期文档] |
| 规范性 | X | 25% | X | [格式、术语、文档结构是否符合软件工程规范] |
| 可追溯性 | X | 20% | X | [需求→架构→详细设计→测试的追溯链是否清晰] |
| 一致性 | X | 15% | X | [与风险管理、设计开发文档是否保持一致] |
| 可操作性 | X | 10% | X | [软件设计描述是否具体可实施，测试方案是否可执行] |
| **综合得分** | | | **X.X/10** | |
"""

    # 4. 注册申报专项审核 — 原子化Rubric增强版
    REGISTRATION_SECTION_PROMPT = """你是一个专业的贴敷式胰岛素泵注册申报审核专家，精通NMPA《胰岛素泵注册审查指导原则》、EU MDR 2017/745和医疗器械注册申报要求。

你的任务是对用户文档的**当前章节**进行深入审核，逐条对照下方审核检查清单，结合知识库参考内容，给出详细的注册申报文档合规分析和修改建议。

## 贴敷式胰岛素泵注册申报特殊关注点
- 产品分类（NMPA III类/MDR Class IIb-III）
- 技术文档（Device Description, Specifications, Drawings）
- 临床评价报告（CER）和临床试验数据
- 产品说明书和标签（IFU/Labeling）
- 风险管理报告（ISO 14971完整报告）
- 软件文档（IEC 62304全套文档）
- 生物相容性评价（ISO 10993系列）
- 电气安全和EMC测试报告（IEC 60601系列）
- 灭菌验证和包装验证
- 有效期和稳定性研究
- 可用性工程文档（IEC 62366）
- GSP/GCP合规性

## 审核检查清单（逐条对照，标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用）

### 1. 注册申报基础文档
- [ ] 1.1 是否明确了目标注册市场和产品分类路径？
- [ ] 1.2 申请表和证明性文件是否齐全？
- [ ] 1.3 注册代理/授权代表是否明确（如适用）？

### 2. 综述资料 (NMPA 注册审查指导原则 第二部分)
- [ ] 2.1 是否包含产品概述（结构组成/工作原理/适用范围）？
- [ ] 2.2 是否描述了贴敷式胰岛素泵的型号规格和性能指标？
- [ ] 2.3 是否包含包装说明和标签样稿？
- [ ] 2.4 适用范围和禁忌症是否明确？
- [ ] 2.5 是否对比了同类已上市产品？

### 3. 研究资料
- [ ] 3.1 性能研究资料是否完整？（输注精度/输注速率范围/报警功能/防水等级）
- [ ] 3.2 生物相容性评价报告是否齐全？（ISO 10993系列）
- [ ] 3.3 灭菌验证资料是否完整？（EO灭菌/辐照灭菌确认方案和报告）
- [ ] 3.4 产品有效期和包装验证资料是否齐全？
- [ ] 3.5 软件研究资料是否完整？（IEC 62304全套文档）
- [ ] 3.6 网络安全研究资料是否齐全？

### 4. 临床评价资料 (NMPA/MDR 附录XIV)
- [ ] 4.1 是否编写了临床评价报告（CER）？
- [ ] 4.2 临床证据是否充分？（是否有临床试验数据或等同性论证）
- [ ] 4.3 CER是否遵循了MEDDEV 2.7/1 rev.4或MDCG 2020系列指南？
- [ ] 4.4 临床试验方案和报告是否符合GCP要求？
- [ ] 4.5 是否评估了剩余临床风险和不确定性？

### 5. 产品风险管理资料
- [ ] 5.1 风险管理报告是否符合ISO 14971:2019要求？
- [ ] 5.2 风险管理是否贯穿产品全生命周期？
- [ ] 5.3 是否识别和评估了贴敷式胰岛素泵特有风险？

### 6. 产品技术要求
- [ ] 6.1 产品技术要求是否完整？
- [ ] 6.2 是否引用或包含了适用的国家标准/行业标准？
- [ ] 6.3 检验方法是否明确、可重复？

### 7. 说明书和标签 (NMPA/MDR附录I)
- [ ] 7.1 产品说明书是否符合法规要求？（内容完整/语言中文）
- [ ] 7.2 标签是否包含所有必需信息？（产品名称/型号/批号/有效期/UDI）
- [ ] 7.3 IFU是否包含充分的用户操作指导和安全警示？
- [ ] 7.4 是否考虑了贴敷式胰岛素泵的特殊使用说明？（佩戴/更换/充电/报警处理）

### 8. MDR技术文档检查（如适用）
- [ ] 8.1 是否编写了MDR附录II技术文档？
- [ ] 8.2 GSPR检查表（EP清单）是否完整？
- [ ] 8.3 是否指定了PRRC（法规合规负责人）？
- [ ] 8.4 PMS和PMCF计划是否建立？

### 9. 第三方检测报告
- [ ] 9.1 电气安全和EMC检测报告是否在有效期内？
- [ ] 9.2 生物相容性试验报告是否由有资质的第三方出具？
- [ ] 9.3 注册检验报告是否齐全？

### 10. 文档规范性
- [ ] 10.1 技术文档之间是否存在逻辑一致性？（风险管理/临床评价/说明书之间不矛盾）
- [ ] 10.2 引用标准是否均为最新有效版本？
- [ ] 10.3 各文档是否有版本控制和审批记录？

## 审核原则
1. 逐条对照上述检查清单进行审核，每条标注符合状态（✅/⚠️/❌/—）
2. ❌和⚠️项必须展开详细的差距分析
3. 差距分析需引用具体法规和指导原则条款
4. 修改建议需包含可操作的示例措辞

## 输出格式（严格按此格式）

### 原文摘要
[摘录用户文档中该章节的关键内容，200字以内]

### 标准要求
[列出注册法规和指导原则的对应要求，注明来源]

### 逐条检查结果
[以上述检查清单格式逐条标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用，并给出具体分析]

### 差距分析
[对每个⚠️和❌项逐条分析注册申报文档的差距，标注对应的检查项编号]

### 修改建议
[对每条差距给出具体建议和示例表述。
**建议N**（对应检查项X.X）: [具体建议]
**示例**: [示例表述]
]

### 关联法规条款
[引用NMPA注册审查指导原则/MDR具体条款]

### 严重度评级
[🔴 严重缺失 / 🟡 需要修改 / 🟢 基本符合]

### 章节量化评分
| 评分维度 | 得分(0-10) | 权重 | 加权分 | 说明 |
|----------|-----------|------|--------|------|
| 完整性 | X | 30% | X | [是否覆盖注册申报要求的所有文件类型] |
| 规范性 | X | 25% | X | [格式、术语、文档结构是否符合注册审查要求] |
| 可追溯性 | X | 20% | X | [技术文档间的引用和追溯关系是否清晰] |
| 一致性 | X | 15% | X | [各文档之间是否无矛盾（如CER与风险管理报告）] |
| 可操作性 | X | 10% | X | [申报资料是否具体可提交，非空泛描述] |
| **综合得分** | | | **X.X/10** | |
"""

    # 5. 生产质量专项审核 — 原子化Rubric增强版
    PRODUCTION_QUALITY_SECTION_PROMPT = """你是一个专业的贴敷式胰岛素泵生产质量管理审核专家，精通ISO 13485:2016第7.5条、NMPA GMP和医疗器械生产质量管理规范。

你的任务是对用户文档的**当前章节**进行深入审核，逐条对照下方审核检查清单，结合知识库参考内容，给出详细的生产质量合规分析和修改建议。

## 贴敷式胰岛素泵生产质量特殊关注点
- 洁净车间管理（ISO 14644/无菌医疗器械生产）
- 微量输注精度检测（流速校准/精度验证）
- 无菌生产工艺（EO灭菌/辐照灭菌验证）
- 关键工序和特殊过程验证（注塑/焊接/装配/灭菌）
- 原材料和零部件控制（储液器材料/敷贴材料/电子元器件）
- 批生产记录（BMR）的完整性和可追溯性
- 过程检验和成品检验（IPC/FQC）
- 生产设备和工装管理
- 标识和可追溯性（UDI要求）
- 产品防护（存储/运输/包装验证）
- 供应商管理和外协加工控制

## 审核检查清单（逐条对照，标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用）

### 1. 机构与人员 (NMPA GMP 第二章)
- [ ] 1.1 是否建立了与生产质量管理相适应的组织架构？
- [ ] 1.2 关键岗位人员（生产负责人/质量负责人/技术负责人）的资质要求是否明确？
- [ ] 1.3 是否有人员培训和考核记录体系？
- [ ] 1.4 洁净区工作人员是否进行健康管理和定期体检？

### 2. 厂房与设施 (NMPA GMP 第三章)
- [ ] 2.1 生产厂房布局是否合理？（人流/物流/气流的区分和控制）
- [ ] 2.2 洁净区环境控制是否满足ISO 14644要求？
- [ ] 2.3 是否有环境监测计划？（温湿度/压差/悬浮粒子/微生物）
- [ ] 2.4 有无菌/灭菌产品生产的隔离措施？
- [ ] 2.5 贴敷式胰岛素泵生产的特殊设施要求是否满足？（防静电/电子组装区/精密装配区）
- [ ] 2.6 仓储设施是否满足物料存储要求？（温控/防潮/防尘/标识管理）

### 3. 设备管理 (NMPA GMP 第四章)
- [ ] 3.1 生产设备是否建立了台账管理和维护计划？
- [ ] 3.2 关键设备是否经过验证和确认（IQ/OQ/PQ）？
- [ ] 3.3 检测设备是否经过校准并在有效期内？
- [ ] 3.4 是否有设备使用和清洁SOP？
- [ ] 3.5 贴敷式胰岛素泵特殊生产设备是否满足要求：（微量输注校准台/泵体组装线/焊接设备/灭菌柜）

### 4. 物料管理 (NMPA GMP 第五章)
- [ ] 4.1 是否有合格供应商清单和管理程序？
- [ ] 4.2 来料检验标准是否明确？（含接收准则和抽样方案）
- [ ] 4.3 关键物料是否建立了追溯体系？（储液器材料/敷贴材料/电子元器件批号追溯）
- [ ] 4.4 物料存储条件是否满足要求？（温度/湿度/有效期/先进先出管理）
- [ ] 4.5 不合格物料的隔离和处理是否有规定？

### 5. 生产管理 (NMPA GMP 第六章)
- [ ] 5.1 是否编制了工艺流程图？
- [ ] 5.2 是否编制了各工序的生产作业指导书（WI）？
- [ ] 5.3 批生产记录（BMR）是否完整且可追溯？
- [ ] 5.4 是否有清场管理和状态标识制度？
- [ ] 5.5 贴敷式胰岛素泵关键工序是否有专门控制：
  - [ ] 5.5.1 储液器组装和密封检测
  - [ ] 5.5.2 输注管路组装和通畅性检测
  - [ ] 5.5.3 泵体PCBA焊接和功能测试
  - [ ] 5.5.4 敷贴材料涂布和粘性检测
  - [ ] 5.5.5 成品输注精度全检测试
  - [ ] 5.5.6 无菌屏障包装和封口验证

### 6. 质量控制 (NMPA GMP 第七章)
- [ ] 6.1 是否有进货检验/过程检验/成品检验的完整检验规范？
- [ ] 6.2 检验规范是否包含检验项目、方法、频率和接收标准？
- [ ] 6.3 是否有留样管理和稳定性考察制度？
- [ ] 6.4 OOS（超标结果）处理流程是否建立？

### 7. 验证与确认 (ISO 13485 §7.5.6)
- [ ] 7.1 工艺验证是否覆盖所有关键工序？（IQ/OQ/PQ完整）
- [ ] 7.2 特殊过程验证是否充分？（灭菌/焊接/注塑/敷贴涂布）
- [ ] 7.3 软件验证是否实施？（生产设备软件/检测设备软件）
- [ ] 7.4 再验证周期是否规定？

### 8. 文件与记录管理
- [ ] 8.1 生产质量文件是否有版本控制和审批流程？
- [ ] 8.2 批记录是否按规定保存？（保存期限≥产品有效期+1年）
- [ ] 8.3 数据完整性是否有保障？（电子记录审计追踪/备份）

### 9. 偏差/CAPA/变更控制
- [ ] 9.1 是否有偏差处理程序？
- [ ] 9.2 是否有纠正和预防措施（CAPA）程序？
- [ ] 9.3 是否有变更控制程序（涉及工艺/设备/物料变更）？
- [ ] 9.4 偏差/CAPA/变更的记录是否完整可追溯？

### 10. UDI与产品追溯
- [ ] 10.1 是否实施了UDI标识管理系统？
- [ ] 10.2 是否建立了从原材料到成品再到分销的全链条追溯？

## 审核原则
1. 逐条对照上述检查清单进行审核，每条标注符合状态（✅/⚠️/❌/—）
2. ❌和⚠️项必须展开详细的差距分析
3. 差距分析需引用GMP/ISO 13485具体条款
4. 修改建议需包含可操作的示例措辞

## 输出格式（严格按此格式）

### 原文摘要
[摘录用户文档中该章节的关键内容，200字以内]

### 标准要求
[列出GMP和ISO 13485对应要求，注明来源]

### 逐条检查结果
[以上述检查清单格式逐条标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用，并给出具体分析]

### 差距分析
[对每个⚠️和❌项逐条分析生产质量文档的差距，标注对应的检查项编号]

### 修改建议
[对每条差距给出具体建议和示例表述。
**建议N**（对应检查项X.X）: [具体建议]
**示例**: [示例表述]
]

### 关联法规条款
[引用GMP/ISO 13485具体条款]

### 严重度评级
[🔴 严重缺失 / 🟡 需要修改 / 🟢 基本符合]

### 章节量化评分
| 评分维度 | 得分(0-10) | 权重 | 加权分 | 说明 |
|----------|-----------|------|--------|------|
| 完整性 | X | 30% | X | [是否覆盖GMP要求的全部生产质量要素] |
| 规范性 | X | 25% | X | [格式、术语、文档结构是否符合GMP规范] |
| 可追溯性 | X | 20% | X | [批记录/物料追溯/设备追溯是否清晰] |
| 一致性 | X | 15% | X | [与设计开发、风险管理文档是否保持一致] |
| 可操作性 | X | 10% | X | [SOP和检验规范是否具体可执行] |
| **综合得分** | | | **X.X/10** | |
"""

    # 6. 体系建设专项审核 — 原子化Rubric增强版
    SYSTEM_CONSTRUCTION_SECTION_PROMPT = """你是一个专业的贴敷式胰岛素泵企业质量管理体系建设审核专家，精通ISO 13485:2016全体系要求和医疗器械质量管理体系搭建。

你的任务是对用户文档的**当前章节**进行深入审核，逐条对照下方审核检查清单，结合知识库参考内容，给出详细的体系建设合规分析和修改建议。

## 贴敷式胰岛素泵体系建设特殊关注点
- 质量手册（覆盖ISO 13485全部条款）
- 文件控制体系（文件编写/审批/分发/修订/作废）
- 记录控制体系（记录表单/归档/保存期限）
- 管理职责和管理评审
- 人力资源管理（岗位资质/培训/健康管理）
- 基础设施管理（厂房/设备/信息系统）
- 工作环境管理（洁净区/温湿度/防静电）
- 采购和供应商管理体系
- 生产过程控制体系
- 监视和测量设备管理（校准/检定）
- 内部审核体系
- 不合格品控制和纠正预防措施（CAPA）
- 顾客反馈和投诉处理
- 不良事件报告和召回程序
- 数据分析与持续改进

## 审核检查清单（逐条对照，标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用）

### 1. 质量管理体系总要求 (ISO 13485:2016 §4.1)
- [ ] 1.1 是否按ISO 13485:2016要求建立了质量管理体系并形成文件？
- [ ] 1.2 质量管理体系的范围是否明确界定？
- [ ] 1.3 是否识别了质量管理体系所需的过程及其相互作用？
- [ ] 1.4 是否确定了这些过程的准则和方法？
- [ ] 1.5 外包过程是否得到识别和控制？

### 2. 文件要求 (ISO 13485:2016 §4.2)
- [ ] 2.1 质量手册是否覆盖ISO 13485全部适用条款？
- [ ] 2.2 质量手册是否描述了质量管理体系的范围和过程相互作用？
- [ ] 2.3 是否建立了文件控制程序？（编写/审批/分发/修订/作废/外来文件管理）
- [ ] 2.4 是否建立了记录控制程序？（标识/存储/保护/检索/保存期限/处置）
- [ ] 2.5 是否建立了贴敷式胰岛素泵产品专用的DHF/DMR文件管理要求？
- [ ] 2.6 文件体系是否层次清晰？（质量手册→程序文件→作业指导书→记录表单）

### 3. 管理职责 (ISO 13485:2016 §5)
- [ ] 3.1 最高管理者是否承诺建立和改进质量管理体系？
- [ ] 3.2 质量方针是否形成文件并经过批准？
- [ ] 3.3 职责和权限是否明确并沟通？
- [ ] 3.4 是否指定了管理者代表？
- [ ] 3.5 是否建立了内部沟通机制？
- [ ] 3.6 是否按计划进行管理评审？（§5.6）

### 4. 资源管理 (ISO 13485:2016 §6)
- [ ] 4.1 是否确定了所需的人力资源并确保胜任能力？
- [ ] 4.2 是否有培训管理体系（培训需求识别/培训计划/培训记录/有效性评价）？
- [ ] 4.3 是否确定了所需的基础设施（厂房/设备/信息系统/支持服务）？
- [ ] 4.4 是否确定了所需的工作环境（洁净区/温湿度/防静电/人身安全）？
- [ ] 4.5 贴敷式胰岛素泵特殊环境控制是否考虑？（电子组装防静电/精密装配环境/无菌生产环境）

### 5. 产品实现 — 策划 (ISO 13485:2016 §7.1)
- [ ] 5.1 是否编制了产品实现策划文件？
- [ ] 5.2 产品实现策划是否与质量管理体系其他过程要求一致？
- [ ] 5.3 是否确定了产品的质量目标和要求？

### 6. 采购与供应商管理 (ISO 13485:2016 §7.4)
- [ ] 6.1 是否有合格供应商清单和评价准则？
- [ ] 6.2 是否有供应商审核和管理程序？
- [ ] 6.3 采购信息是否充分（规格/要求/验收准则）？
- [ ] 6.4 是否有采购产品的验证程序？

### 7. 监视和测量设备管理 (ISO 13485:2016 §7.6)
- [ ] 7.1 是否建立了监视和测量设备管理程序？
- [ ] 7.2 是否有设备校准/检定计划和记录？
- [ ] 7.3 校准是否可追溯到国际或国家测量标准？

### 8. 测量、分析和改进 (ISO 13485:2016 §8)
- [ ] 8.1 是否建立了顾客反馈和投诉处理程序？（§8.2.1）
- [ ] 8.2 是否建立了内部审核程序并按计划实施？（§8.2.2-8.2.3）
- [ ] 8.3 是否建立了过程和产品的监视测量程序？（§8.2.4-8.2.5）
- [ ] 8.4 是否建立了不合格品控制程序？（§8.3）
- [ ] 8.5 是否建立了数据分析程序？（§8.4）
- [ ] 8.6 是否建立了纠正措施程序（CAPA）？（§8.5.2）
- [ ] 8.7 是否建立了预防措施程序？（§8.5.3）

### 9. 贴敷式胰岛素泵企业特殊体系要求
- [ ] 9.1 是否建立了软件生命周期管理融入QMS的程序？
- [ ] 9.2 是否建立了网络安全事件响应流程？
- [ ] 9.3 是否建立了不良事件报告和FSCA（现场安全纠正措施）程序？
- [ ] 9.4 是否建立了上市后监督（PMS）体系？
- [ ] 9.5 是否建立了临床评价和PMCF（上市后临床跟踪）程序？
- [ ] 9.6 是否建立了UDI实施和追溯体系？
- [ ] 9.7 是否考虑了无菌医疗器械的附加体系要求？

### 10. 文件体系层次结构检查
- [ ] 10.1 质量手册是否引用了下层级程序文件？
- [ ] 10.2 程序文件是否引用了更下层的作业指导书和记录表单？
- [ ] 10.3 各层级文件之间的接口和引用关系是否清晰？

### 11. 文档规范性
- [ ] 11.1 体系文件是否有版本控制和审批记录？
- [ ] 11.2 文件分发和收回是否有记录？
- [ ] 11.3 作废文件是否有标识和隔离措施？
- [ ] 11.4 是否有文件定期评审和更新机制？

## 审核原则
1. 逐条对照上述检查清单进行审核，每条标注符合状态（✅/⚠️/❌/—）
2. ❌和⚠️项必须展开详细的差距分析
3. 差距分析需引用ISO 13485:2016具体条款号
4. 修改建议需包含可操作的示例措辞

## 输出格式（严格按此格式）

### 原文摘要
[摘录用户文档中该章节的关键内容，200字以内]

### 标准要求
[列出ISO 13485对应条款要求，注明来源]

### 逐条检查结果
[以上述检查清单格式逐条标注：✅已满足 / ⚠️部分满足 / ❌未满足 / —不适用，并给出具体分析]

### 差距分析
[对每个⚠️和❌项逐条分析体系文件的差距，标注对应的检查项编号]

### 修改建议
[对每条差距给出具体建议和示例表述。
**建议N**（对应检查项X.X）: [具体建议]
**示例**: [示例表述]
]

### 关联法规条款
[引用ISO 13485:2016具体条款号]

### 严重度评级
[🔴 严重缺失 / 🟡 需要修改 / 🟢 基本符合]

### 章节量化评分
| 评分维度 | 得分(0-10) | 权重 | 加权分 | 说明 |
|----------|-----------|------|--------|------|
| 完整性 | X | 30% | X | [是否覆盖ISO 13485全部适用条款] |
| 规范性 | X | 25% | X | [格式、术语、文档结构是否符合QMS规范] |
| 可追溯性 | X | 20% | X | [体系文件层级间的引用和追溯关系是否清晰] |
| 一致性 | X | 15% | X | [体系文件之间是否无矛盾、协调一致] |
| 可操作性 | X | 10% | X | [体系要求是否具体可执行，非空泛口号] |
| **综合得分** | | | **X.X/10** | |
"""

    # 通用体系审核（保底使用）— 增强版
    GENERAL_SECTION_PROMPT = """你是一个专业的贴敷式胰岛素泵生产企业体系文件审核专家。

你的任务是对用户文档的**当前章节**进行深入审核，结合知识库参考内容给出修改建议。

## 审核范围
- ISO 13485:2016 医疗器械质量管理体系
- ISO 14971:2019 医疗器械风险管理
- IEC 62304 医疗器械软件生命周期过程
- EU MDR 2017/745 欧盟医疗器械法规
- NMPA 医疗器械生产质量管理规范
- NMPA 胰岛素泵注册审查指导原则

## 贴敷式胰岛素泵特殊关注点
- 微量输注精度和安全性
- 软件控制系统的可靠性
- 无菌耗材的生物相容性和灭菌
- 用户界面的人因工程
- 蓝牙通信的网络安全

## 审核原则
1. 完整性：是否覆盖相关法规条款
2. 一致性：内容是否相互协调
3. 可操作性：描述是否足够具体
4. 证据链：是否有记录表单支撑
5. 产品特异性：是否充分考虑贴敷式胰岛素泵的特有风险

## 输出格式（严格按此格式）

### 原文摘要
[摘录用户文档中该章节的关键内容，200字以内]

### 标准要求
[列出知识库中对应的要求，注明来源]

### 差距分析
[逐条分析差距]

### 修改建议
[对每条差距给出具体建议和示例表述。
**建议N**: [具体建议]
**示例**: [示例表述]
]

### 关联法规条款
[引用相关标准条款]

### 严重度评级
[🔴 严重缺失 / 🟡 需要修改 / 🟢 基本符合]

### 章节量化评分
| 评分维度 | 得分(0-10) | 权重 | 加权分 | 说明 |
|----------|-----------|------|--------|------|
| 完整性 | X | 30% | X | [是否覆盖标准要求的所有要素] |
| 规范性 | X | 25% | X | [格式、术语、表述是否符合行业规范] |
| 可追溯性 | X | 20% | X | [内容是否可追溯到相关上游文档] |
| 一致性 | X | 15% | X | [与文档内其他章节和相关文档是否一致] |
| 可操作性 | X | 10% | X | [描述是否具体可执行，非空泛口号] |
| **综合得分** | | | **X.X/10** | |
"""

    # ============== 六大领域综合分析 Prompts ==============

    RISK_MANAGEMENT_SYNTHESIS_PROMPT = """你是一个专业的贴敷式胰岛素泵风险管理审核专家。

你已完成了对用户风险管理文档各章节的逐一审核（包含逐条检查清单结果、5维量化评分和严重度评级）。现在需要综合所有章节的审核结果，生成最终的风险管理审核报告。

## 输入说明
你将收到各章节的结构化审核摘要，每个章节包含：章节标题、量化评分（5维）、严重发现（🔴/🟡）、关键缺失项、关联条款。请充分利用这些信息生成综合报告。

## 输出格式

# 贴敷式胰岛素泵风险管理文档审核报告

## 一、审核概述
- 文档类型和审核范围
- 审核依据：ISO 14971:2019 / YY/T 0316 / GB/T 42062-2022
- 总体风险管理合规情况综述

## 二、各章节评分总览
| 章节 | 完整性(30%) | 规范性(25%) | 可追溯性(20%) | 一致性(15%) | 可操作性(10%) | 综合得分 |
|------|------------|------------|-------------|-----------|-------------|---------|
| [章节名] | X/10 | X/10 | X/10 | X/10 | X/10 | X.X/10 |
| ... | | | | | | |
| **文档总体得分** | | | | | | **X.X/10** |

## 三、严重问题汇总（🔴 严重缺失）
[列出所有严重缺失项，每项包含：所在章节、检查项编号、问题描述、相关条款、修改建议]

## 四、需要修改项汇总（🟡 需要修改）
[列出所有需要修改项，按检查清单领域分组]

## 五、基本符合项（🟢 基本符合）

## 六、贴敷式胰岛素泵特有风险覆盖检查
[逐项检查以下特有风险是否在文档中得到充分识别和控制：
- 过量输注风险（over-delivery）— 控制算法失效/电机失控
- 欠量输注风险（under-delivery）— 管路堵塞/储液器气泡/电机失步
- 输注管路堵塞/泄漏
- 电池耗尽/故障
- 蓝牙通信中断/数据错误
- 移动APP崩溃/响应延迟
- 敷贴材料过敏/皮肤刺激
- 储液器气泡/泄漏
- 用户操作错误
- 网络安全攻击/数据泄露
- 电磁兼容干扰
]

## 七、缺失风险管理文档
[列出文档中应该有但没有的风险管理文件类型]

## 八、交叉引用与追溯链检查
[检查：危害识别→风险估计→风险控制→剩余风险评价的追溯链是否完整]

## 九、修改优先级建议
[按严重度+影响范围排序，给出修改优先级：
**P0（立即修改）**: [涉及安全有效性的严重缺失]
**P1（本周修改）**: [涉及法规合规的重要缺失]
**P2（本月修改）**: [涉及文档规范性的改进项]
]

## 十、关联法规条款总览
[汇总ISO 14971:2019/YY/T 0316/GB/T 42062-2022涉及的所有条款]
## 十一、总体评分与合规等级
| 总得分 | 合规等级 | 说明 |
|--------|---------|------|
| X.X/10 | [优秀(≥8.5) / 良好(7-8.4) / 基本合格(5.5-6.9) / 不合格(<5.5)] | [总体评价] |
"""

    DESIGN_DEV_SYNTHESIS_PROMPT = """你是一个专业的贴敷式胰岛素泵设计开发审核专家。

你已完成了对用户设计开发文档各章节的逐一审核（包含逐条检查清单结果、5维量化评分和严重度评级）。现在需要综合所有章节的审核结果，生成最终的设计开发审核报告。

## 输入说明
你将收到各章节的结构化审核摘要，每个章节包含：章节标题、量化评分（5维）、严重发现（🔴/🟡）、关键缺失项、关联条款。请充分利用这些信息生成综合报告。

## 输出格式

# 贴敷式胰岛素泵设计开发文档审核报告

## 一、审核概述
- 文档类型和审核范围
- 审核依据：ISO 13485:2016 §7.3 设计开发控制
- 总体设计控制合规情况综述

## 二、各章节评分总览
| 章节 | 完整性(30%) | 规范性(25%) | 可追溯性(20%) | 一致性(15%) | 可操作性(10%) | 综合得分 |
|------|------------|------------|-------------|-----------|-------------|---------|
| [章节名] | X/10 | X/10 | X/10 | X/10 | X/10 | X.X/10 |
| ... | | | | | | |
| **文档总体得分** | | | | | | **X.X/10** |

## 三、严重问题汇总（🔴 严重缺失）
[列出所有严重缺失项，每项包含：所在章节、检查项编号、问题描述、相关条款、修改建议]

## 四、需要修改项汇总（🟡 需要修改）
[按设计控制阶段分组：策划/输入/输出/评审/验证/转换/确认/变更]

## 五、基本符合项（🟢 基本符合）

## 六、DHF/DMR完整性检查
[逐项检查设计历史文档(DHF)和器械主记录(DMR)应包含的关键文档：
**设计策划**: 项目开发计划书 / 市场调研与产品定义 / 可行性研究报告 / 专利分析 / 立项评审 / 注册路径策略 / 风险管理计划
**设计输入**: 用户需求 / 设计输入文件 / 硬件需求 / 结构需求 / 软件需求 / 包装需求 / 产品需求追溯矩阵(RTM)
**设计输出（DHF）**: 硬件方案 / 结构方案 / 软件方案 / 软件编码规范 / 包装方案 / 初包装材料确认报告 / 设计输出清单 / 产品技术要求
**设计输出（DMR）**: BOM表 / 物料规格书及图纸 / 产品图纸(总装图/爆炸图/零件图/原理图) / 工艺流程图 / 设备清单及验证SOP / 工装图纸及验收记录 / 检验规范(进货/过程/出厂) / 生产工艺作业指导书 / 软件版本包 / 标签及使用说明书
**设计验证**: 验证计划 / 性能验证 / 输注精度验证 / 包装及标识验证 / 使用期限验证 / 货架有效期验证 / 包装运输验证 / 可沥滤物测试 / 检验方法学验证
**设计转换**: 转换计划 / 转换报告 / 工艺验证计划 / 灭菌确认方案及报告
**设计确认**: 确认方案及报告 / 临床试验方案及报告 / 可用性测试方案及报告
**设计评审与变更**: 各阶段设计评审记录 / 设计变更记录
**供应商管理**: 合格供应商清单 / 供应商资质及审核报告
**第三方检测**: 生物相容性试验报告 / 药液相容性试验报告 / 安规EMC环境可靠性检测报告 / 注册检验报告
]

## 七、设计追溯矩阵检查
[检查设计输入→设计输出→设计验证→设计确认的追溯链完整性]

## 八、缺失文档/章节
## 九、修改优先级建议
[按严重度+影响范围排序：
**P0（立即修改）**: [涉及产品安全和有效性的严重缺失]
**P1（本周修改）**: [涉及设计控制合规的重要缺失]
**P2（本月修改）**: [涉及文档规范性和完整性的改进项]
]

## 十、关联法规条款总览
## 十一、总体评分与合规等级
| 总得分 | 合规等级 | 说明 |
|--------|---------|------|
| X.X/10 | [优秀(≥8.5) / 良好(7-8.4) / 基本合格(5.5-6.9) / 不合格(<5.5)] | [总体评价] |
"""

    SOFTWARE_COMPLIANCE_SYNTHESIS_PROMPT = """你是一个专业的贴敷式胰岛素泵软件合规审核专家。

你已完成了对用户软件文档各章节的逐一审核（包含逐条检查清单结果、5维量化评分和严重度评级）。现在需要综合所有章节的审核结果，生成最终的软件合规审核报告。

## 输入说明
你将收到各章节的结构化审核摘要，每个章节包含：章节标题、量化评分（5维）、严重发现（🔴/🟡）、关键缺失项、关联条款。请充分利用这些信息生成综合报告。

## 输出格式

# 贴敷式胰岛素泵软件合规审核报告

## 一、审核概述
- 软件系统概述和审核范围
- 软件安全级别判定合理性分析（控制输注的固件应为C级）
- 审核依据：IEC 62304 / YY/T 0664
- 总体软件合规情况综述

## 二、各章节评分总览
| 章节 | 完整性(30%) | 规范性(25%) | 可追溯性(20%) | 一致性(15%) | 可操作性(10%) | 综合得分 |
|------|------------|------------|-------------|-----------|-------------|---------|
| [章节名] | X/10 | X/10 | X/10 | X/10 | X/10 | X.X/10 |
| ... | | | | | | |
| **文档总体得分** | | | | | | **X.X/10** |

## 三、严重问题汇总（🔴 严重缺失）
[每项包含：所在章节、检查项编号、问题描述、IEC 62304条款、修改建议]

## 四、需要修改项汇总（🟡 需要修改）
[按IEC 62304章节分组]

## 五、基本符合项（🟢 基本符合）

## 六、IEC 62304文档完整性检查
[逐项检查应包含的软件文档：
- 软件开发计划（SDP）— §5.1
- 软件需求规格（SRS）— §5.2
- 软件架构设计文档 — §5.3
- 软件详细设计文档 — §5.4
- 软件单元测试方案/报告 — §5.5-5.6
- 软件集成测试方案/报告 — §5.7
- 软件系统测试方案/报告 — §5.8
- 软件发布记录 — §5.8.8
- 软件配置管理计划 — §6
- 软件问题解决报告 — §7
- 软件维护计划 — §8
- 风险管理在软件中的应用 — §9
]

## 七、贴敷式胰岛素泵软件特有合规检查
[检查：蓝牙协议安全文档/移动APP合规/OTA升级风控/网络安全威胁建模/闭环算法安全/SOUP管理]

## 八、缺失文档/章节
## 九、修改优先级建议
[**P0**: 涉及患者安全的软件缺陷 | **P1**: 涉及法规合规的重要缺失 | **P2**: 文档规范性改进]

## 十、关联法规条款总览
## 十一、总体评分与合规等级
| 总得分 | 合规等级 | 说明 |
|--------|---------|------|
| X.X/10 | [优秀(≥8.5) / 良好(7-8.4) / 基本合格(5.5-6.9) / 不合格(<5.5)] | [总体评价] |
"""

    REGISTRATION_SYNTHESIS_PROMPT = """你是一个专业的贴敷式胰岛素泵注册申报审核专家。

你已完成了对用户注册申报文档各章节的逐一审核（包含逐条检查清单结果、5维量化评分和严重度评级）。现在需要综合所有章节的审核结果，生成最终的注册申报审核报告。

## 输入说明
你将收到各章节的结构化审核摘要，每个章节包含：章节标题、量化评分（5维）、严重发现（🔴/🟡）、关键缺失项、关联条款。请充分利用这些信息生成综合报告。

## 输出格式

# 贴敷式胰岛素泵注册申报文档审核报告

## 一、审核概述
- 目标注册市场（NMPA/MDR/FDA）和产品分类
- 审核依据：NMPA注册审查指导原则 / EU MDR 2017/745（如适用）
- 总体注册申报准备情况综述

## 二、各章节评分总览
| 章节 | 完整性(30%) | 规范性(25%) | 可追溯性(20%) | 一致性(15%) | 可操作性(10%) | 综合得分 |
|------|------------|------------|-------------|-----------|-------------|---------|
| [章节名] | X/10 | X/10 | X/10 | X/10 | X/10 | X.X/10 |
| ... | | | | | | |
| **文档总体得分** | | | | | | **X.X/10** |

## 三、严重问题汇总（🔴 严重缺失）
[每项包含：所在章节、检查项编号、问题描述、相关法规条款、修改建议]

## 四、需要修改项汇总（🟡 需要修改）
[按注册申报资料类型分组]

## 五、基本符合项（🟢 基本符合）

## 六、NMPA注册申报资料完整性检查
[对照《胰岛素泵注册审查指导原则》逐项检查：
- 申请表和证明性文件
- 综述资料（产品概述/结构组成/适用范围）
- 研究资料（性能研究/生物相容性/灭菌/有效期/软件研究）
- 生产制造信息
- 临床评价资料
- 产品风险分析资料
- 产品技术要求
- 产品说明书和标签样稿
- 质量管理体系文件
]

## 七、MDR技术文档检查（如适用）
## 八、缺失文档/章节
## 九、修改优先级建议
[**P0**: 影响注册受理的严重缺失 | **P1**: 需要补正的重要缺失 | **P2**: 文档规范性改进]

## 十、关联法规条款总览
## 十一、总体评分与合规等级
| 总得分 | 合规等级 | 说明 |
|--------|---------|------|
| X.X/10 | [优秀(≥8.5) / 良好(7-8.4) / 基本合格(5.5-6.9) / 不合格(<5.5)] | [总体评价] |
"""

    PRODUCTION_QUALITY_SYNTHESIS_PROMPT = """你是一个专业的贴敷式胰岛素泵生产质量管理审核专家。

你已完成了对用户生产质量文档各章节的逐一审核（包含逐条检查清单结果、5维量化评分和严重度评级）。现在需要综合所有章节的审核结果，生成最终的生产质量审核报告。

## 输入说明
你将收到各章节的结构化审核摘要，每个章节包含：章节标题、量化评分（5维）、严重发现（🔴/🟡）、关键缺失项、关联条款。请充分利用这些信息生成综合报告。

## 输出格式

# 贴敷式胰岛素泵生产质量管理文档审核报告

## 一、审核概述
- 生产场所和范围
- 审核依据：NMPA GMP / ISO 13485:2016 §7.5
- 总体生产质量合规情况综述

## 二、各章节评分总览
| 章节 | 完整性(30%) | 规范性(25%) | 可追溯性(20%) | 一致性(15%) | 可操作性(10%) | 综合得分 |
|------|------------|------------|-------------|-----------|-------------|---------|
| [章节名] | X/10 | X/10 | X/10 | X/10 | X/10 | X.X/10 |
| ... | | | | | | |
| **文档总体得分** | | | | | | **X.X/10** |

## 三、严重问题汇总（🔴 严重缺失）
[每项包含：所在章节、检查项编号、问题描述、GMP条款、修改建议]

## 四、需要修改项汇总（🟡 需要修改）
[按GMP章节分组]

## 五、基本符合项（🟢 基本符合）

## 六、GMP关键要素检查
[逐项检查：
- 机构和人员（组织架构/关键岗位资质/培训记录）
- 厂房与设施（洁净区管理/环境监测/设施维护）
- 设备管理（生产设备/检测设备/设备验证）
- 物料管理（原料采购/来料检验/仓储管理/标识追溯）
- 生产管理（生产工艺/批记录/清场/状态标识）
- 质量控制（QC实验室/检验标准/留样管理/OOS处理）
- 验证与确认（工艺验证/设备确认/软件验证/灭菌验证）
- 文件管理（文件体系/记录管理/数据完整性）
- 质量保证（偏差/CAPA/变更控制/年度质量回顾）
]

## 七、贴敷式胰岛素泵特殊工艺检查
[检查：微量输注精度校准/无菌屏障组装/灭菌工艺验证/敷贴材料涂布/电子模块焊接测试/成品输注精度全检测试]

## 八、缺失文档/章节
## 九、修改优先级建议
[**P0**: 影响产品安全有效的严重缺失 | **P1**: 涉及GMP合规的重要缺失 | **P2**: 文档规范性改进]

## 十、关联法规条款总览
## 十一、总体评分与合规等级
| 总得分 | 合规等级 | 说明 |
|--------|---------|------|
| X.X/10 | [优秀(≥8.5) / 良好(7-8.4) / 基本合格(5.5-6.9) / 不合格(<5.5)] | [总体评价] |
"""

    SYSTEM_CONSTRUCTION_SYNTHESIS_PROMPT = """你是一个专业的贴敷式胰岛素泵企业质量管理体系建设审核专家。

你已完成了对用户体系文件各章节的逐一审核（包含逐条检查清单结果、5维量化评分和严重度评级）。现在需要综合所有章节的审核结果，生成最终的体系建设审核报告。

## 输入说明
你将收到各章节的结构化审核摘要，每个章节包含：章节标题、量化评分（5维）、严重发现（🔴/🟡）、关键缺失项、关联条款。请充分利用这些信息生成综合报告。

## 输出格式

# 贴敷式胰岛素泵质量管理体系建设审核报告

## 一、审核概述
- 体系建设范围和现状
- 审核依据：ISO 13485:2016 全体系
- 总体体系建设合规情况综述

## 二、各章节评分总览
| 章节 | 完整性(30%) | 规范性(25%) | 可追溯性(20%) | 一致性(15%) | 可操作性(10%) | 综合得分 |
|------|------------|------------|-------------|-----------|-------------|---------|
| [章节名] | X/10 | X/10 | X/10 | X/10 | X/10 | X.X/10 |
| ... | | | | | | |
| **文档总体得分** | | | | | | **X.X/10** |

## 三、严重问题汇总（🔴 严重缺失）
[每项包含：所在章节、检查项编号、问题描述、ISO 13485条款、修改建议]

## 四、需要修改项汇总（🟡 需要修改）
[按ISO 13485章节分组]

## 五、基本符合项（🟢 基本符合）

## 六、ISO 13485:2016全条款覆盖检查
[对照ISO 13485:2016各条款逐条检查体系文件覆盖情况：
- 4.1-4.2 质量管理体系总要求和文件要求
- 5.1-5.6 管理职责
- 6.1-6.4 资源管理
- 7.1-7.6 产品实现
- 8.1-8.5 测量、分析和改进
]

## 七、贴敷式胰岛素泵企业体系特殊要求
[检查：
- 软件生命周期管理融入QMS
- 网络安全事件响应流程
- 不良事件报告和FSCA流程
- 上市后监督（PMS）体系
- 临床评价和PMCF流程
- UDI实施和追溯体系
- 无菌医疗器械特殊管理
- 委托生产/外协管理（如适用）
]

## 八、文件体系层次结构检查
[质量手册→程序文件→作业指导书→记录表单的四级文件体系完整性]

## 九、缺失文档/章节
## 十、修改优先级建议
[**P0**: 影响体系有效运行的严重缺失 | **P1**: 涉及认证审核的重要缺失 | **P2**: 文档规范性改进]

## 十一、关联法规条款总览
## 十二、总体评分与合规等级
| 总得分 | 合规等级 | 说明 |
|--------|---------|------|
| X.X/10 | [优秀(≥8.5) / 良好(7-8.4) / 基本合格(5.5-6.9) / 不合格(<5.5)] | [总体评价] |
"""

    # 通用综合分析（保底使用）— 增强版
    SYNTHESIS_PROMPT = """你是一个专业的贴敷式胰岛素泵生产企业体系文件审核专家。

你已完成了对用户文档各章节的逐一审核（包含5维量化评分和严重度评级）。现在需要综合所有章节的审核结果，生成最终审核报告。

## 贴敷式胰岛素泵审核背景
本次审核针对贴敷式胰岛素泵（patch insulin pump）生产企业的内部文档。贴敷式胰岛素泵是一种可穿戴的胰岛素持续皮下输注设备，包含泵体、储液器、输注管路、控制系统和配套移动APP。审核中需特别关注微量输注精度、软件可靠性、无菌屏障完整性、生物相容性、蓝牙通信安全性等产品特有风险。

## 输入说明
你将收到各章节的结构化审核摘要，每个章节包含：章节标题、量化评分（5维）、严重发现（🔴/🟡）、关键缺失项、关联条款。请充分利用这些信息生成综合报告。

## 输出格式

# 贴敷式胰岛素泵体系文件审核报告

## 一、审核概述
- 文档类型、审核范围和审核依据
- 审核涉及的标准和法规
- 总体合规情况概述

## 二、各章节评分总览
| 章节 | 完整性(30%) | 规范性(25%) | 可追溯性(20%) | 一致性(15%) | 可操作性(10%) | 综合得分 |
|------|------------|------------|-------------|-----------|-------------|---------|
| [章节名] | X/10 | X/10 | X/10 | X/10 | X/10 | X.X/10 |
| ... | | | | | | |
| **文档总体得分** | | | | | | **X.X/10** |

## 三、严重问题汇总（🔴 严重缺失）
[列出所有严重缺失项，每项包含：所在章节、问题描述、相关条款、修改建议]

## 四、需要修改项汇总（🟡 需要修改）

## 五、基本符合项（🟢 基本符合）

## 六、缺失章节/文档
[列出文档中应该有但没有的章节]

## 七、交叉引用问题
[检查各章节之间的一致性问题]

## 八、贴敷式胰岛素泵特有风险评估
[检查文档是否充分考虑了贴敷式胰岛素泵的特有风险：
- 微量输注精度和安全性
- 软件控制系统的可靠性
- 无菌屏障完整性
- 生物相容性
- 蓝牙通信安全性
]

## 九、修改优先级建议
[**P0（立即修改）**: 涉及安全有效性的严重缺失]
[**P1（本周修改）**: 涉及法规合规的重要缺失]
[**P2（本月修改）**: 涉及文档规范性的改进项]

## 十、关联法规条款总览
## 十一、总体评分与合规等级
| 总得分 | 合规等级 | 说明 |
|--------|---------|------|
| X.X/10 | [优秀(≥8.5) / 良好(7-8.4) / 基本合格(5.5-6.9) / 不合格(<5.5)] | [总体评价] |
"""

    # ============== Prompt 选择映射 ==============
    SECTION_PROMPTS = {
        "risk_management": RISK_MANAGEMENT_SECTION_PROMPT,
        "design_dev": DESIGN_DEV_SECTION_PROMPT,
        "software_compliance": SOFTWARE_COMPLIANCE_SECTION_PROMPT,
        "registration": REGISTRATION_SECTION_PROMPT,
        "production_quality": PRODUCTION_QUALITY_SECTION_PROMPT,
        "system_construction": SYSTEM_CONSTRUCTION_SECTION_PROMPT,
        "general": GENERAL_SECTION_PROMPT,
    }

    SYNTHESIS_PROMPTS = {
        "risk_management": RISK_MANAGEMENT_SYNTHESIS_PROMPT,
        "design_dev": DESIGN_DEV_SYNTHESIS_PROMPT,
        "software_compliance": SOFTWARE_COMPLIANCE_SYNTHESIS_PROMPT,
        "registration": REGISTRATION_SYNTHESIS_PROMPT,
        "production_quality": PRODUCTION_QUALITY_SYNTHESIS_PROMPT,
        "system_construction": SYSTEM_CONSTRUCTION_SYNTHESIS_PROMPT,
        "general": SYNTHESIS_PROMPT,
    }

    def __init__(self, vector_store, api_key: str, api_url: str, model: str = "deepseek-v4-pro"):
        self.vector_store = vector_store
        self.api_key = api_key
        self.api_url = api_url
        self.model = model
        # 共享 httpx.AsyncClient 实例，避免并发时重复创建
        self._http_client: Optional[httpx.AsyncClient] = None
        # 线程锁，序列化 ChromaDB HNSW 索引查询，避免多线程同时加载索引导致 OOM
        self._chroma_lock = threading.Lock()

    async def _get_http_client(self) -> httpx.AsyncClient:
        """获取或创建共享的 httpx.AsyncClient"""
        if self._http_client is None or self._http_client.is_closed:
            self._http_client = httpx.AsyncClient(timeout=600, trust_env=False)
        return self._http_client

    async def close(self):
        """关闭共享的 httpx 客户端"""
        if self._http_client and not self._http_client.is_closed:
            await self._http_client.aclose()
            self._http_client = None

    def _retrieve_for_section_sync(self, section_text: str, n_results: int = 5) -> List[Dict[str, Any]]:
        """
        针对特定段落检索相关的知识库内容（同步方法，供 run_in_executor 调用）

        使用线程锁序列化 ChromaDB 查询，避免多线程同时加载 HNSW 索引导致 OOM

        Args:
            section_text: 段落文本
            n_results: 返回结果数量

        Returns:
            相关文档片段列表
        """
        # 序列化 ChromaDB 查询，防止多个线程同时加载 HNSW 索引
        with self._chroma_lock:
            results = self.vector_store.query(
                query_texts=[section_text],
                n_results=n_results * 2
            )

        docs = []
        if results and 'documents' in results:
            for i, doc_text in enumerate(results['documents'][0]):
                metadata = results['metadatas'][0][i] if 'metadatas' in results else {}
                # 从metadata构建source显示字符串：优先使用source_file，兼容source字段
                source = metadata.get("source_file", metadata.get("source", ""))
                source_subdir = metadata.get("source_subdir", "")
                section_title = metadata.get("section_title", "")
                if not source:
                    source = source_subdir or section_title or "unknown"
                # 构建用于显示的完整来源信息
                display_source = source
                if source_subdir:
                    display_source = f"{source_subdir}/{source}"
                if section_title:
                    display_source = f"{display_source} [{section_title}]"
                source_lower = display_source.lower()
                # 优先保留与审核领域相关的文档（覆盖六大领域）
                domain_keywords = [
                    '风险', 'risk', '14971', '42062', '9706',   # 风险管理
                    '设计', 'design', 'dhf', '13485',            # 设计开发
                    '软件', 'software', '62304', 'sdp',          # 软件合规
                    '注册', 'registration', 'mdr', '临床',       # 注册申报
                    '生产', 'production', 'gmp', '质量',         # 生产质量
                    '体系', 'system', '手册', '程序',             # 体系建设
                    '胰岛素', 'insulin', 'pump', '泵',           # 产品特异性
                ]
                if any(kw in source_lower for kw in domain_keywords):
                    docs.append({
                        "text": doc_text,
                        "source": display_source,
                        "chunk_id": metadata.get("chunk_id", i)
                    })
                    if len(docs) >= n_results:
                        break

        # 补充其他文档
        if len(docs) < n_results:
            for i, doc_text in enumerate(results['documents'][0]):
                if len(docs) >= n_results:
                    break
                metadata = results['metadatas'][0][i] if 'metadatas' in results else {}
                source = metadata.get("source_file", metadata.get("source", ""))
                source_subdir = metadata.get("source_subdir", "")
                section_title = metadata.get("section_title", "")
                if not source:
                    source = source_subdir or section_title or "unknown"
                display_source = source
                if source_subdir:
                    display_source = f"{source_subdir}/{source}"
                if section_title:
                    display_source = f"{display_source} [{section_title}]"
                if not any(d['source'] == display_source for d in docs):
                    docs.append({
                        "text": doc_text,
                        "source": display_source,
                        "chunk_id": metadata.get("chunk_id", i)
                    })

        return docs

    async def _retrieve_for_section(self, section_text: str, n_results: int = 5) -> List[Dict[str, Any]]:
        """
        异步检索知识库内容（包装同步 ChromaDB 调用，避免阻塞事件循环）

        Args:
            section_text: 段落文本
            n_results: 返回结果数量

        Returns:
            相关文档片段列表
        """
        loop = asyncio.get_event_loop()
        return await loop.run_in_executor(
            None,
            self._retrieve_for_section_sync,
            section_text,
            n_results
        )

    async def _call_llm(self, system_prompt: str, user_content: str, max_tokens: int = 4000, temperature: float = 0.7) -> str:
        """
        调用 LLM API，带重试机制

        Args:
            system_prompt: 系统提示词
            user_content: 用户内容
            max_tokens: 最大输出 token 数
            temperature: 温度参数

        Returns:
            LLM 回答文本
        """
        headers = {
            "Content-Type": "application/json",
            "Authorization": f"Bearer {self.api_key}"
        }

        payload = {
            "model": self.model,
            "messages": [
                {"role": "system", "content": system_prompt},
                {"role": "user", "content": user_content}
            ],
            "temperature": temperature,
            "max_tokens": max_tokens
        }

        client = await self._get_http_client()

        # 重试逻辑：最多3次，指数退避
        max_retries = 3
        for attempt in range(max_retries):
            try:
                response = await client.post(self.api_url, headers=headers, json=payload)
                response.raise_for_status()
                # 安全解析 JSON：API 可能返回非 JSON 错误文本
                try:
                    result = response.json()
                except json.JSONDecodeError:
                    response_text = response.text[:500]
                    logger.warning(f"API 返回非 JSON 响应: {response_text}")
                    if attempt < max_retries - 1:
                        wait_time = 2 ** attempt
                        await asyncio.sleep(wait_time)
                        continue
                    raise RuntimeError(f"API 返回了非 JSON 格式的响应: {response_text}")

                answer = ""
                choices = result.get("choices", [])
                if choices and len(choices) > 0:
                    choice = choices[0]
                    message = choice.get("message", {})
                    answer = message.get("content", "")

                return answer or str(result)
            except httpx.HTTPStatusError as e:
                if e.response.status_code == 429:
                    # 速率限制，指数退避
                    wait_time = 2 ** attempt
                    logger.warning(f"API 速率限制，等待 {wait_time} 秒后重试 (尝试 {attempt + 1}/{max_retries})")
                    await asyncio.sleep(wait_time)
                    continue
                raise
            except (httpx.TimeoutException, httpx.ConnectError) as e:
                if attempt < max_retries - 1:
                    wait_time = 2 ** attempt
                    logger.warning(f"API 请求失败: {e}，等待 {wait_time} 秒后重试")
                    await asyncio.sleep(wait_time)
                    continue
                raise

        return ""

    async def _audit_single_section(
        self,
        section_title: str,
        section_content: str,
        section_level: int,
        audit_type: str,
        semaphore: asyncio.Semaphore
    ) -> Dict[str, Any]:
        """
        审核单个章节

        Args:
            section_title: 章节标题
            section_content: 章节内容
            section_level: 标题层级
            audit_type: 审核类型
            semaphore: 并发信号量

        Returns:
            包含审核结果的字典
        """
        async with semaphore:
            try:
                # 检索相关知识库内容
                relevant_docs = await self._retrieve_for_section(section_content, n_results=5)

                # 选择对应领域的 system prompt（六大领域 + general 保底）
                system_prompt = self.SECTION_PROMPTS.get(audit_type, self.GENERAL_SECTION_PROMPT)

                # 构建每章节的上下文
                context_parts = [
                    f"## 当前审核章节：{section_title}\n",
                    "### 用户文档原文：",
                    section_content[:5000],
                    "\n### 标准要求/参考模板（来自知识库）："
                ]

                if relevant_docs:
                    for i, doc in enumerate(relevant_docs, 1):
                        context_parts.append(f"\n--- 参考 {i} (来源: {doc['source']}) ---")
                        context_parts.append(doc['text'][:2000])
                else:
                    context_parts.append("[未在知识库中找到直接相关的参考内容，请基于标准原文进行审核]")

                user_content = '\n'.join(context_parts)

                # 调用 LLM（Phase 1: max_tokens 4000→6000 适应更长的Rubric输出）
                answer = await self._call_llm(
                    system_prompt=system_prompt,
                    user_content=user_content,
                    max_tokens=6000,
                    temperature=0.7
                )

                # 提取摘要（用于综合分析）
                summary = self._extract_section_summary(answer)

                return {
                    "title": section_title,
                    "level": section_level,
                    "answer": answer,
                    "summary": summary,
                    "relevant_docs": relevant_docs,
                    "status": "success"
                }
            except Exception as e:
                logger.error(f"章节 '{section_title}' 审核失败: {e}")
                return {
                    "title": section_title,
                    "level": section_level,
                    "answer": f"### 审核失败\n\n该章节审核过程中发生错误: {str(e)}",
                    "summary": f"审核失败: {str(e)}",
                    "relevant_docs": [],
                    "status": "error"
                }

    def _extract_section_summary(self, answer: str) -> str:
        """
        从章节审核结果中提取结构化摘要（用于综合分析阶段）

        提取：量化评分、严重度评级、关键发现（❌/⚠️项）、关联条款
        为综合Prompt提供比简单200字摘要更丰富的结构化输入
        """
        lines = answer.split('\n')
        summary_parts = []

        # 1. 提取量化评分表（最高优先级，综合阶段需要汇总打分）
        score_section_started = False
        for line in lines:
            stripped = line.strip()
            if '综合得分' in stripped or '章节量化评分' in stripped:
                score_section_started = True
                summary_parts.append(stripped)
                continue
            if score_section_started:
                if '|' in stripped and ('得分' in stripped or '---' in stripped or any(
                    dim in stripped for dim in ['完整性', '规范性', '可追溯性', '一致性', '可操作性', '综合']
                )):
                    summary_parts.append(stripped)
                elif stripped and not stripped.startswith('|'):
                    score_section_started = False  # 评分表结束

        # 2. 提取逐条检查结果中的 ❌ 和 ⚠️ 项（关键缺失）
        checklist_findings = []
        for line in lines:
            stripped = line.strip()
            if ('❌' in stripped or '⚠️' in stripped) and '- [' in stripped:
                # 限制长度，保留检查项编号和状态
                if len(stripped) > 150:
                    stripped = stripped[:150] + '...'
                checklist_findings.append(stripped)
        if checklist_findings:
            summary_parts.append('\n### 关键缺失/不足项')
            # 最多10条
            for finding in checklist_findings[:10]:
                summary_parts.append(finding)

        # 3. 提取严重度评级行
        severity_lines = []
        for line in lines:
            stripped = line.strip()
            if ('🔴' in stripped or '🟡' in stripped) and '严重缺失' in stripped or '需要修改' in stripped:
                if len(stripped) > 200:
                    stripped = stripped[:200] + '...'
                severity_lines.append(stripped)
        if severity_lines:
            summary_parts.append('\n### 严重度评级')
            for sev in severity_lines[:5]:
                summary_parts.append(sev)

        # 4. 提取关联法规条款
        clause_lines = []
        in_clause_section = False
        for line in lines:
            stripped = line.strip()
            if '关联法规条款' in stripped:
                in_clause_section = True
                continue
            if in_clause_section:
                if stripped.startswith('#'):  # 下一个章节
                    break
                if stripped and len(stripped) > 5:
                    clause_lines.append(stripped)
        if clause_lines:
            summary_parts.append('\n### 关联条款')
            for clause in clause_lines[:5]:
                summary_parts.append(clause)

        # 5. 降级处理：如果什么都没提取到，取前500字
        if not summary_parts:
            return answer[:500].strip()

        return '\n'.join(summary_parts)

    # 文件类型显示名映射（基于DHF清单扩充，覆盖设计控制全生命周期）
    _DOC_TYPE_LABELS = {
        # ===== 风险管理 =====
        'rm_plan': '风险管理计划', 'hazard_id': '危害识别报告', 'risk_analysis': '风险分析报告 (FMEA)',
        'risk_control': '风险控制方案', 'residual_risk': '剩余风险评价报告', 'rm_report': '风险管理报告',
        'prelim_risk_analysis': '初步风险分析', 'risk_analysis_table': '产品风险分析和管理总表',
        'cybersec_risk_table': '网络安全风险分析和管理总表', 'dfmea': 'DFMEA设计失效模式与影响分析',
        'other_rm': '其他风险管理文件',
        # ===== 设计开发（按设计控制阶段组织：策划→输入→输出→验证→转换→确认） =====
        # 设计策划
        'dd_plan': '项目开发计划书', 'market_research': '市场调研与产品定义',
        'feasibility_report': '项目可行性研究报告', 'patent_analysis': '专利分析报告',
        'project_approval': '立项及评审记录', 'registration_strategy': '注册路径策略',
        # 设计输入
        'design_input': '设计输入文件', 'user_requirements': '用户需求',
        'hardware_req': '硬件设计需求', 'structure_req': '结构设计需求',
        'sw_design_req': '软件设计需求', 'packaging_req': '包装及标识设计需求',
        # 设计输出 — DHF
        'design_output': '设计输出文件', 'hardware_design': '硬件设计方案',
        'structure_design': '结构设计方案', 'sw_design': '软件设计方案',
        'sw_coding_std': '软件编码规范', 'packaging_design': '包装及标识设计方案',
        'packaging_material': '初包装材料选择与确认报告', 'design_output_checklist': '设计输出清单',
        # 设计输出 — DMR
        'bom': 'BOM表/物料清单', 'material_spec': '物料规格书/图纸',
        'product_drawings': '产品图纸(总装图/爆炸图/零件图/原理图)',
        'equipment_list': '生产检验设备清单及验证SOP', 'tooling_drawings': '工装图纸及验收记录',
        'inspection_specs': '检验规范(进货/过程/出厂)', 'production_wi': '生产工艺作业指导书',
        'sw_version_pkg': '软件版本包',
        # 设计验证
        'design_verif_plan': '设计验证计划', 'design_verif': '设计验证方案/报告',
        'performance_verif': '性能验证方案/报告', 'infusion_accuracy_verif': '输注准确性性能验证',
        'packaging_verif': '包装及标识验证方案/报告', 'service_life_verif': '使用期限验证方案/报告',
        'shelf_life_verif': '货架有效期验证方案/报告', 'transport_verif': '包装运输验证方案/报告',
        'leachable_test': '可沥滤物测试方案/报告',
        # 设计转换
        'design_transfer_plan': '设计转换计划', 'design_transfer': '设计转换文件',
        'design_transfer_report': '设计转换报告', 'process_valid_plan': '工艺验证计划',
        'sterilization_valid': '灭菌确认方案/报告',
        # 设计确认
        'design_valid': '设计确认方案/报告', 'clinical_trial': '临床试验方案/报告',
        'usability_test': '可用性测试方案/报告',
        # 评审/变更/追溯
        'design_review': '设计评审记录', 'design_change': '设计变更记录',
        'product_rtm': '产品需求追溯矩阵(RTM)',
        # 其它支持文档
        'product_tech_req': '产品技术要求', 'test_method_valid': '检验方法学验证方案/报告',
        'performance_research': '性能研究相关记录', 'qualified_supplier_list': '合格供应商清单',
        'supplier_qualification': '供应商资质及审核报告', 'other_dd': '其他设计开发文件',
        # ===== 软件合规 =====
        'sdp': '软件开发计划(SDP)', 'srs': '软件需求规格(SRS)', 'sadd': '软件架构设计文档(SADD)',
        'sddd': '软件详细设计文档(SDDD)', 'sw_test': '软件单元测试方案/报告',
        'sw_integration_test': '软件集成测试方案/报告', 'sw_system_test': '软件系统测试方案/报告',
        'sw_quality_test': '软件质量测试方案/报告', 'scm_plan': '软件配置管理计划(SCMP)',
        'sw_pr_report': '软件问题解决报告', 'sw_maint': '软件维护计划',
        'soup_mgmt': 'SOUP/OTS管理记录', 'cybersec': '网络安全文档',
        'cybersec_test': '网络安全测试方案/报告', 'sw_interface_cybersec_test': '软件接口网络安全测试方案/报告',
        'sw_trace_matrix': '软件开发追溯表/软件需求追溯矩阵',
        'cybersec_trace': '网络安全追溯表',
        'other_sw': '其他软件合规文件',
        # ===== 注册申报 =====
        'ep_checklist': '医疗器械安全和性能基本原则(EP)清单',
        'tech_req': '产品技术要求', 'overview': '综述资料', 'research': '研究资料',
        'cer': '临床评价报告(CER)', 'ifu_label': '产品说明书/标签',
        'biocomp': '生物相容性评价报告', 'sterile_valid': '灭菌验证报告',
        'stability': '稳定性研究报告/加速老化报告',
        'third_party_biocomp': '生物相容性/药液相容性试验报告(第三方)',
        'third_party_emc': '安规/EMC/环境可靠性检测报告(第三方)',
        'third_party_reg_test': '注册检验报告(第三方)',
        'other_reg': '其他注册申报文件',
        # ===== 生产质量 =====
        'process_flow': '工艺流程图', 'iq_oq_pq': '工艺验证方案/报告(IQ/OQ/PQ)',
        'bmr': '批生产记录(BMR)', 'incoming_insp': '来料检验标准', 'final_insp': '成品检验标准',
        'equipment_mgmt': '设备管理文件', 'supplier_mgmt': '供应商管理文件',
        'sterile_batch': '灭菌批记录', 'udi_mgmt': 'UDI标识管理文件',
        'sterilization_process_valid': '灭菌工艺验证方案/报告',
        'tooling_acceptance': '工装验收记录', 'production_inspection_sop': '生产/检验SOP',
        'other_pq': '其他生产质量文件',
        # ===== 体系建设 =====
        'quality_manual': '质量手册', 'procedure_doc': '程序文件', 'work_instruction': '作业指导书',
        'record_form': '记录表单', 'internal_audit': '内审计划/报告', 'mgmt_review': '管理评审报告',
        'capa_record': 'CAPA记录', 'training_record': '培训记录', 'pms_plan': '上市后监督计划(PMS)',
        'document_control': '文件控制程序',
        'design_control_procedure': '设计控制程序', 'risk_mgmt_procedure': '风险管理程序',
        'sw_dev_procedure': '软件开发程序',
        'other_sc': '其他体系建设文件',
    }

    async def analyze_document(
        self,
        user_document: str,
        user_filename: str,
        temperature: float = 0.7,
        max_tokens: int = 4000,
        use_contrast_mode: bool = True,
        audit_type: str = "risk_management",
        doc_type: str = ""
    ) -> Dict[str, Any]:
        """
        使用多轮审核流水线分析用户文档

        流水线：
        1. 代码分割：按 Markdown 标题切分章节
        2. 逐章并发审核：每章节独立 LLM 调用，asyncio.gather + Semaphore(2)
        3. 综合分析：汇总各章节摘要，生成最终报告

        Args:
            user_document: 用户文档内容（Markdown 格式）
            user_filename: 用户文件名
            temperature: 温度参数
            max_tokens: 每章节最大输出 token 数
            use_contrast_mode: 是否使用对照审核模式
            audit_type: 审核类型
            doc_type: 文件类型代码

        Returns:
            包含完整审核报告的字典
        """
        from doc_processor import split_by_markdown_headers

        # 文件类型中文名
        doc_type_label = self._DOC_TYPE_LABELS.get(doc_type, doc_type) if doc_type else ""

        # ===== 第一轮：代码分割章节 =====
        sections = split_by_markdown_headers(user_document)
        logger.info(f"文档分割为 {len(sections)} 个章节")

        if not sections:
            # 降级到原始模式
            return await self._analyze_document_fallback(user_document, user_filename, audit_type, doc_type_label)

        # 限制最多审核 30 个章节，避免超长文档导致内存溢出
        MAX_SECTIONS = 30
        if len(sections) > MAX_SECTIONS:
            logger.warning(f"文档章节过多({len(sections)})，仅审核前 {MAX_SECTIONS} 个")
            sections = sections[:MAX_SECTIONS]

        # ===== 第二轮：逐章并发审核 =====
        # 限制并发数为 2，避免 ChromaDB HNSW 索引多线程同时加载导致内存溢出
        semaphore = asyncio.Semaphore(2)

        tasks = []
        for title, content, level in sections:
            tasks.append(
                self._audit_single_section(title, content, level, audit_type, semaphore)
            )

        section_results = await asyncio.gather(*tasks)

        # 释放原始文档文本（已分割为章节，不再需要全文）
        del user_document
        gc.collect()

        # 汇总所有检索到的文档（合并去重）
        seen_sources = set()
        all_retrieved_docs = []
        for result in section_results:
            for doc in result.get("relevant_docs", []):
                source = doc.get("source", "")
                if source not in seen_sources:
                    seen_sources.add(source)
                    all_retrieved_docs.append(doc)
            # 清除 relevant_docs 以释放内存（后续只用 summary 和 answer）
            result.pop("relevant_docs", None)

        # ===== 第三轮：综合分析 =====
        try:
            # 构建综合分析上下文（只传摘要，不传全文）
            doc_type_hint = f"\n**审核文件类型**: {doc_type_label}\n" if doc_type_label else ""
            synthesis_parts = [
                f"以下是文档「{user_filename}」{doc_type_hint}各章节的审核摘要：\n"
            ]

            for result in section_results:
                level_prefix = "#" * max(result["level"], 1) if result["level"] > 0 else "##"
                synthesis_parts.append(f"{level_prefix} {result['title']}")
                synthesis_parts.append(result['summary'])
                synthesis_parts.append("")

            # 添加文档章节列表（用于缺失章节检测）
            synthesis_parts.append("\n---\n文档完整章节列表：")
            for result in section_results:
                indent = "  " * (result["level"] - 1) if result["level"] > 0 else ""
                synthesis_parts.append(f"{indent}- {result['title']}")

            synthesis_context = '\n'.join(synthesis_parts)

            synthesis_answer = await self._call_llm(
                system_prompt=self.SYNTHESIS_PROMPTS.get(audit_type, self.SYNTHESIS_PROMPT),
                user_content=synthesis_context,
                max_tokens=12000,
                temperature=0.5
            )
        except Exception as e:
            logger.error(f"综合分析失败: {e}")
            synthesis_answer = "## 综合分析\n\n综合分析阶段发生错误，请参考各章节独立审核结果。"

        # ===== 组装最终报告 =====
        # 内存优化：限制最终报告大小，防止前端浏览器因 DOM 过大而崩溃
        MAX_FINAL_ANSWER_CHARS = 50000  # 最终报告最大字符数
        MAX_SECTION_ANSWER_CHARS = 3000  # 每个章节详情在最终报告中的最大字符数

        final_parts = [synthesis_answer, "\n\n---\n\n# 各章节详细审核结果\n"]

        for i, result in enumerate(section_results, 1):
            level_prefix = "#" * max(result["level"], 1) if result["level"] > 0 else "##"
            final_parts.append(f"\n{level_prefix} 审核点 {i}：{result['title']}\n")
            # 截断过长的章节回答
            section_answer = result['answer']
            if len(section_answer) > MAX_SECTION_ANSWER_CHARS:
                section_answer = section_answer[:MAX_SECTION_ANSWER_CHARS] + f"\n\n... (该章节审核结果共 {len(result['answer'])} 字符，已截断。完整结果请参见上方综合分析)"
            final_parts.append(section_answer)
            final_parts.append("\n---\n")

        final_answer = '\n'.join(final_parts)

        # 内存保护：最终报告超出上限时进一步截断
        if len(final_answer) > MAX_FINAL_ANSWER_CHARS:
            logger.warning(f"最终报告过大({len(final_answer)}字符)，截断至{MAX_FINAL_ANSWER_CHARS}字符")
            final_answer = final_answer[:MAX_FINAL_ANSWER_CHARS] + (
                f"\n\n---\n\n⚠️ 审核报告过长，共 {len(final_answer)} 字符，"
                f"已截断至 {MAX_FINAL_ANSWER_CHARS} 字符。"
                f"共审核了 {len(sections)} 个章节，详细结果请查看上方各章节分析。"
            )

        result = {
            "answer": final_answer,
            "section_count": len(sections),
            "section_results": [
                {"title": r["title"], "status": r["status"]}
                for r in section_results
            ],
            "retrieved_docs": all_retrieved_docs
        }

        # 显式释放大对象以帮助 GC
        final_parts.clear()
        synthesis_parts.clear()
        for r in section_results:
            r.clear()
        section_results.clear()
        gc.collect()

        return result

    async def _analyze_document_fallback(
        self,
        user_document: str,
        user_filename: str,
        audit_type: str,
        doc_type_label: str = ""
    ) -> Dict[str, Any]:
        """
        降级模式：当文档无法分割章节时，使用单次调用审核

        Args:
            user_document: 用户文档内容
            user_filename: 用户文件名
            audit_type: 审核类型
            doc_type_label: 文件类型中文名

        Returns:
            审核结果
        """
        # 选择对应领域的 system prompt
        system_prompt = self.SECTION_PROMPTS.get(audit_type, self.GENERAL_SECTION_PROMPT)

        # 简单检索
        relevant_docs = await self._retrieve_for_section(user_document, n_results=5)

        doc_type_info = f"\n**审核文件类型**: {doc_type_label}" if doc_type_label else ""
        context_parts = [
            f"文档名: {user_filename}{doc_type_info}\n",
            "### 用户文档原文：",
            user_document[:8000],
            "\n### 标准要求/参考模板（来自知识库）："
        ]
        for i, doc in enumerate(relevant_docs, 1):
            context_parts.append(f"\n--- 参考 {i} (来源: {doc['source']}) ---")
            context_parts.append(doc['text'][:2000])

        user_content = '\n'.join(context_parts)

        answer = await self._call_llm(
            system_prompt=system_prompt,
            user_content=user_content,
            max_tokens=8000,
            temperature=0.7
        )

        return {
            "answer": answer,
            "section_count": 1,
            "section_results": [{"title": "完整文档", "status": "success"}],
            "retrieved_docs": relevant_docs
        }

    # ============== 兼容旧接口 ==============
    # ============== 逐段审核（Segment-by-Segment Review） ==============
    # 段落审核 Prompt 前缀：告诉 LLM 当前正在审核文档的某个段落
    SEGMENT_AUDIT_PREFIX = """你是一个专业的贴敷式胰岛素泵生产企业体系文件审核专家。

你正在对一份文档的**第 {segment_index} 段（共 {total_segments} 段）**进行审核。
这段内容从文档的第 {start_pos} 个字符开始，到第 {end_pos} 个字符结束。

请对这段内容进行详细审核，结合知识库中的标准要求，给出专业的修改建议。
输出格式要求：
1. 先简要概述本段内容
2. 逐条列出审核发现（✅已满足 / ⚠️部分满足 / ❌未满足）
3. 给出具体的修改建议
4. 标注涉及的相关法规条款

注意：只审核本段内容，不要审核整份文档。"""

    async def audit_segment(
        self,
        segment_text: str,
        segment_index: int,
        total_segments: int,
        start_pos: int,
        end_pos: int,
        audit_type: str,
        doc_type_label: str = ""
    ) -> Dict[str, Any]:
        """
        审核文档的单个文本段落（逐段审核模式）

        Args:
            segment_text: 本段文本内容
            segment_index: 当前段落索引（1-based）
            total_segments: 总段落数
            start_pos: 本段在文档中的起始字符位置
            end_pos: 本段在文档中的结束字符位置
            audit_type: 审核类型
            doc_type_label: 文件类型中文名

        Returns:
            包含审核结果的字典
        """
        try:
            # 检索相关知识库内容
            relevant_docs = await self._retrieve_for_section(segment_text, n_results=5)

            # 选择对应领域的 system prompt
            domain_prompt = self.SECTION_PROMPTS.get(audit_type, self.GENERAL_SECTION_PROMPT)

            # 构建段落审核的 system prompt
            segment_hint = self.SEGMENT_AUDIT_PREFIX.format(
                segment_index=segment_index,
                total_segments=total_segments,
                start_pos=start_pos,
                end_pos=end_pos
            )
            system_prompt = segment_hint + "\n\n" + domain_prompt

            # 构建上下文
            doc_type_info = f"\n**审核文件类型**: {doc_type_label}" if doc_type_label else ""
            context_parts = [
                f"## 当前审核段落：第 {segment_index}/{total_segments} 段{doc_type_info}\n",
                "### 本段原文：",
                segment_text,
                "\n### 标准要求/参考模板（来自知识库）："
            ]

            if relevant_docs:
                for i, doc in enumerate(relevant_docs, 1):
                    context_parts.append(f"\n--- 参考 {i} (来源: {doc['source']}) ---")
                    context_parts.append(doc['text'][:2000])
            else:
                context_parts.append("[未在知识库中找到直接相关的参考内容，请基于标准原文进行审核]")

            user_content = '\n'.join(context_parts)

            # 调用 LLM
            answer = await self._call_llm(
                system_prompt=system_prompt,
                user_content=user_content,
                max_tokens=6000,
                temperature=0.7
            )

            return {
                "segment_index": segment_index,
                "start_pos": start_pos,
                "end_pos": end_pos,
                "answer": answer,
                "relevant_docs": relevant_docs,
                "status": "success"
            }
        except Exception as e:
            logger.error(f"段落 {segment_index} 审核失败: {e}")
            return {
                "segment_index": segment_index,
                "start_pos": start_pos,
                "end_pos": end_pos,
                "answer": f"### 审核失败\n\n该段落审核过程中发生错误: {str(e)}",
                "relevant_docs": [],
                "status": "error"
            }

    def retrieve_relevant_docs(self, query_text: str, n_results: int = 5) -> List[Dict[str, Any]]:
        """兼容旧接口：从知识库检索相关片段"""
        return self._retrieve_for_section_sync(query_text, n_results=n_results)

    def build_context(self, user_document: str, user_filename: str, n_results: int = 5, use_contrast_mode: bool = True) -> Tuple[str, List[Dict[str, Any]]]:
        """兼容旧接口：构建增强上下文"""
        relevant_docs = self._retrieve_for_section_sync(user_document, n_results=n_results)
        context_parts = [
            "=" * 60,
            "【用户上传的体系文件】",
            f"文件名: {user_filename}",
            "=" * 60,
            user_document[:8000],
            "\n",
            "=" * 60,
            "【知识库检索结果 - 相关法规和模板参考】",
            "=" * 60
        ]
        for i, doc in enumerate(relevant_docs, 1):
            context_parts.append(f"\n--- 参考文档 {i} (来源: {doc['source']}) ---")
            context_parts.append(doc['text'][:2000])
        return '\n'.join(context_parts), relevant_docs


def create_rag_retriever(vector_store, api_key: str, api_url: str, model: str = "deepseek-v4-pro") -> RAGRetriever:
    """
    工厂函数：创建 RAG 检索器实例

    Args:
        vector_store: VectorStore 实例
        api_key: API 密钥
        api_url: API 地址
        model: 模型名称

    Returns:
        RAGRetriever 实例
    """
    return RAGRetriever(
        vector_store=vector_store,
        api_key=api_key,
        api_url=api_url,
        model=model
    )
