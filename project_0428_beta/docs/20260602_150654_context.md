# 会话上下文记录

**保存时间:** 2026-06-02 15:06:54
**工作目录:** E:\nrf_sample_codes\working_team_work\public\project\project_0428_beta
**保存路径:** E:\nrf_sample_codes\working_team_work\public\project\project_0428_beta\docs\20260602_150654_context.md

## 对话详情

### 用户:
（继续上一轮会话）上一轮会话完成了贴敷式胰岛素泵文档审核数字员工的6大领域改造，包括风险管理、设计开发、软件合规、注册申报、生产质量、体系建设，以及级联文件类型下拉选择器。

### AI:
验证了项目关键文件状态，两个Python文件语法校验通过，项目处于干净状态。询问用户下一步操作。

### 用户:
为什么参考的知识库文档显示unknown

### AI:
排查发现根因：知识库构建脚本 `build_insulin_pump_kb.py` 在存入 ChromaDB 时，元数据字段名用的是 `source_file`，但 `rag_retriever.py` 检索时读的字段名是 `source`。两个字段名不匹配，`metadata.get("source", "unknown")` 永远拿不到值，全部显示为 `unknown`。

**修复内容**（`backend/rag_retriever.py:680-720`）：
- 将 `metadata.get("source", "unknown")` 改为 `metadata.get("source_file", metadata.get("source", ""))`
- 利用元数据中的 `source_subdir`（子目录）和 `section_title`（章节标题）拼出更完整的来源显示
- 修复了两处相同问题（优先匹配领域关键词的循环和补充文档的循环）

语法校验通过。

### 用户:
中文解释

### AI:
用中文简要解释了根因和修复方式。

### 用户:
帮我改一下

### AI:
确认修改已在上一步完成，语法校验通过。建议启动服务验证效果。

## 关键信息

- **Bug根因**: ChromaDB 元数据字段名不一致 — 写入用 `source_file`，读取用 `source`
- **影响范围**: `rag_retriever.py` 的 `_retrieve_for_section_sync` 方法中两处 `metadata.get("source", "unknown")`
- **修复文件**: `backend/rag_retriever.py`
- **修复后显示格式**: `源子目录/文件名 [章节标题]`，例如 `标准/ISO14971-2019.pdf [风险管理流程]`

## 决策记录

- 采用 `source_file` 为主键、`source` 为兼容回退、`source_subdir`/`section_title` 为补充的优先级链，最大程度利用已有元数据
- 不做额外的知识库重建或元数据迁移，仅在检索时做字段映射修复，最小改动原则
